# Thomas Codebase Issue Queue

Raw, deduped issue candidates found by Thomas codebase issue researcher passes.

## 2026-06-26

### deliverable-play-url-ignores-attempt-boundaries

- **Paths:** `thomas/server/chat_delegation.py`; `thomas/server/chat_delegation_deliverable.py`; `thomas/server/routes/deliverable_aiohttp.py`; `plans/thomas/chat_stress_2026-06-17/FINDINGS_ADVERSARIAL.md`
- **Evidence:** `chat_delegation.py` creates a reusable per-execution workspace with `mkdir(parents=True, exist_ok=True)` and does not clear it between retry attempts. `chat_delegation_deliverable.py` explicitly snapshots workspace mtimes so summaries exclude untouched orphan files from failed prior attempts. `deliverable_aiohttp.py:131-145` still chooses the served entry by scanning all files in the workspace, with no attempt baseline. The stress findings at `plans/thomas/chat_stress_2026-06-17/FINDINGS_ADVERSARIAL.md:99-105` describe and confirm the same mismatch.
- **Impact:** A failed first attempt can leave `index.html`; a later successful attempt can produce `game-v2.html`; the summary can honestly report the new file while the user-facing Play link opens the stale broken file.
- **Suggested fix shape:** Carry the successful attempt baseline or selected deliverable path into the completed execution record, then have `deliverable_entry` prefer that recorded path before falling back to workspace-wide discovery. Add a regression that writes stale `index.html`, captures a baseline, writes `game-v2.html`, and asserts the served entry is the new file.
- **Risk:** Medium. Touches user-visible deliverable serving, but the fix can be isolated to recorded metadata plus route selection.
- **Estimated effort:** 0.5-1 day.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to the deliverable route and focused tests.

### task-runtime-mark-reported-refreshes-updated-at

- **Paths:** `thomas/core/task_bot_runtime.py`; `tests/test_task_bot_runtime.py`; `thomas/server/web/js/runtime/013_actions_interactions_02.js`; `plans/thomas/chat_stress_2026-06-17/FINDINGS_ADVERSARIAL.md`
- **Evidence:** `task_bot_runtime.py:459` unconditionally sets `payload["updated_at"] = now_iso`. Lines 461-475 define `pure_reported_write` and avoid refreshing `last_heartbeat_at`, but not `updated_at`. `tests/test_task_bot_runtime.py:38-53` only asserts the heartbeat is preserved and does not assert `updated_at` stability. The stress finding at `plans/thomas/chat_stress_2026-06-17/FINDINGS_ADVERSARIAL.md:107-112` documents the resulting recency-window bug.
- **Impact:** A chat-side mark-reported bookkeeping write can make an old completed row look freshly updated to frontend polling and relative-time logic, corrupting stale/recency behavior for finished delegations.
- **Suggested fix shape:** Preserve `updated_at` on pure reported writes, or introduce a separate bookkeeping timestamp. Extend `test_pure_reported_flag_write_does_not_refresh_heartbeat` to capture and assert unchanged `updated_at`.
- **Risk:** Medium. Timestamp semantics may affect UI polling and Mission Control displays, so tests should cover terminal-row behavior.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### release-hygiene-gate-red-and-tests-depend-on-live-release-state

- **Paths:** `scripts/forge/gates/release_hygiene.py`; `tests/test_release_hygiene.py`; `CHANGELOG.md`; `pyproject.toml`; `thomas/__init__.py`; `docs/THREAT_MODEL_WEB_API.md`; `scripts/security_audit.py`
- **Evidence:** Running `python scripts/forge/gates/release_hygiene.py` from the live repo fails with `CHANGELOG.md is missing a section header for version [0.16.13]`, `security audit failed (high severity): threat_model_cadence`, onboarding warning `events=14, required>=20`, and security warning count 1. The live release metadata currently reports `pyproject.toml:7` as `version = "0.16.13"` and `thomas/__init__.py:5` as `__version__ = "0.16.13"`, while the first changelog version headers are `## [Unreleased]` and `## [0.16.11] - 2026-05-29`. Running `python scripts/security_audit.py --repo-root . --json --strict` fails `threat_model_cadence`: `docs/THREAT_MODEL_WEB_API.md` has `last_reviewed` `2026-05-19`, file age 35.08 days, and max age 30. Running `python -m pytest tests/test_release_hygiene.py -q` reports `4 failed, 4 passed`; the failing tests expect warning-only or disabled onboarding/security sub-gates to return 0, but `release_hygiene.run()` always performs the live version/changelog checks before those mocked branches, so the focused tests inherit unrelated live repo release state.
- **Impact:** The release hygiene gate is currently red, so publish/merge lanes that enforce it cannot pass. The focused unit tests also fail for environmental release-state reasons, which makes it harder to safely change onboarding/security behavior without first fixing unrelated changelog or threat-model cadence state.
- **Suggested fix shape:** Reconcile release metadata by adding the missing `[0.16.13]` changelog section or correcting the version bump, and refresh/review `docs/THREAT_MODEL_WEB_API.md` so `threat_model_cadence` passes. Then refactor `tests/test_release_hygiene.py` to isolate or monkeypatch the base version/changelog checks when the test is specifically about onboarding/security warning behavior, while keeping separate tests for version and changelog enforcement.
- **Risk:** Medium-high. Release hygiene and security cadence are publish boundaries, so the fix should not blindly suppress failures; it needs explicit changelog and threat-model review evidence.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to release hygiene metadata/cadence reconciliation and focused tests.

### bible-script-catalog-still-describes-deleted-check-gate-family

- **Paths:** `docs/THOMAS_BIBLE.md`; `scripts/crew/brief/commit.py`; `scripts/forge/gates/`
- **Evidence:** `docs/THOMAS_BIBLE.md:6714` says Section 29 counted `53 check_*` scripts and spot-checked `check_ai_workflow_contract`, `check_competitive_scope_gate`, `check_placeholder_completion_policy`, `check_mutating_route_policy_exceptions`, and `check_surface_parity` as real. `docs/THOMAS_BIBLE.md:6734-6736` says the `53-script check_* family` is invoked from `agent_commit.py` and/or publish preflight. `docs/THOMAS_BIBLE.md:6768-6769` says each script is real and CLI-invokable, and `docs/THOMAS_BIBLE.md:6831-6845` still tells future agents to add `check_*` scripts and treats `agent_commit.py` as the protected orchestrator. Live repo evidence disagrees: `Get-ChildItem scripts -File -Filter 'check_*.py'` returns count `0`, `rg --files scripts | rg '(^|/)check_.*\.py$'` returns no paths, and `Test-Path scripts/agent_commit.py` is false. The current commit gate list is in `scripts/crew/brief/commit.py:32-109` and invokes `scripts/forge/gates/*` entries; `Get-ChildItem scripts/forge/gates -File -Filter '*.py'` returns 57 gate modules.
- **Impact:** Section 29 is one of the repo's orientation maps, but it directs agents toward a deleted top-level gate family and a deleted protected commit orchestrator. Agents following it may create new checks in the wrong location, search for nonexistent scripts, or misunderstand which commit gates are actually authoritative.
- **Suggested fix shape:** Re-audit Section 29 against the current `scripts/crew/brief/commit.py` gate list and `scripts/forge/gates/` modules. Replace the stale `53 check_*` and `agent_commit.py` guidance with the current `scripts/forge/gates/*` taxonomy, note any intentional compatibility/rename history separately, and add a small doc freshness check that fails when Bible script-catalog examples reference missing live paths as current.
- **Risk:** Low-medium. This is documentation-only, but it is a high-traffic operational map and should be updated carefully to preserve historical rename context without presenting old paths as live.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to docs plus a focused freshness check.

### precommit-skip-policy-tests-leak-quickbuilder-state

- **Paths:** `tests/test_precommit_skip_policy_gate.py`; `scripts/forge/gates/precommit_skip_policy.py`; `docs/QUICKBUILDER.md`
- **Evidence:** In the live checkout, `python -c "from scripts.forge.gates.precommit_skip_policy import _quickbuilder_active; print(_quickbuilder_active())"` prints `True`. Running `python -m pytest tests/test_precommit_skip_policy_gate.py::test_fails_when_breakglass_cooldown_active tests/test_precommit_skip_policy_gate.py::test_fails_when_breakglass_quota_exceeded tests/test_precommit_skip_policy_gate.py::test_breakglass_quota_binds_to_verified_actor_not_agent_id_B5 -q` reports all three tests failed because `mod.run(...)` returned `0` instead of the expected `1`. The gate intentionally waives cooldown and per-actor quota when QuickBuilder is active at `scripts/forge/gates/precommit_skip_policy.py:621-668`, and `docs/QUICKBUILDER.md:3-31` documents that QuickBuilder relaxes those checks and stays on until manually disabled. The tests monkeypatch `_now_utc`, staged files, and breakglass authorization, but they do not monkeypatch `_quickbuilder_active`, so their expected cooldown/quota failures depend on external machine state.
- **Impact:** A developer or automation run with QuickBuilder enabled gets a red focused precommit-skip-policy suite even though the gate is behaving as documented for that mode. That makes breakglass-policy regression tests non-hermetic and can hide real policy regressions behind local environment state.
- **Suggested fix shape:** In cooldown/quota tests, monkeypatch `mod._quickbuilder_active` to `lambda: False` so they verify the default rate-limit path deterministically. Add separate focused coverage for QuickBuilder-active behavior that asserts cooldown/quota are waived and that output/audit metadata makes the waiver explicit. Consider a fixture that defaults QuickBuilder off for all precommit skip policy tests unless a test opts in.
- **Risk:** Low-medium. The change is test-scoped, but it covers sensitive bypass policy, so the positive QuickBuilder waiver tests should preserve the intended human-activated behavior.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### security-audit-cli-json-polluted-by-tool-extension-warning

- **Paths:** `thomas/cli/_commands_misc.py`; `thomas/server/tool_extensions.py`; `thomas/marketplace/security/security_audit.py`; `tests/test_cli_governance_release_ecosystem.py`
- **Evidence:** Running `python -m pytest tests/test_cli_governance_release_ecosystem.py::test_cli_security_audit_json -q` fails because `json.loads(result.output)` raises `JSONDecodeError`. The captured Click output begins with `12:28:56 [thomas.server.tool_extensions] WARNING: Loaded only 21/136 optional tool modules; expected at least 100. This may indicate registry drift or a broken Thomas install.` before the JSON object. The tested command is `thomas.cli._commands_misc.security_audit_cmd` (`thomas/cli/_commands_misc.py:193-224`), which calls `run_security_audit(...)` and emits JSON with `_emit_json(payload, ensure_ascii=False)`. The warning originates from the optional tool registry threshold in `thomas/server/tool_extensions.py` (`_OPTIONAL_TOOL_LOW_LOAD_WARNING_THRESHOLD = 100`) and is emitted during the security audit path, so `thomas security audit --json` is not machine-parseable under the current CLI test harness.
- **Impact:** The security audit CLI advertises `--json` but can produce non-JSON stdout. Automation that consumes `thomas security audit --json`, including release/governance checks, can fail before reading the audit payload, and the focused CLI governance test is red.
- **Suggested fix shape:** Keep `--json` stdout reserved for the JSON payload. Route optional tool load warnings to stderr or suppress/delay noisy registry warnings when running JSON CLI commands. Add regression coverage asserting `result.output` starts with `{` for `security audit --json`, and optionally assert the low-load warning still appears in human mode or stderr where appropriate.
- **Risk:** Medium. The warning is useful operator signal, but it must not corrupt machine-readable output; fix should preserve visibility outside JSON stdout.
- **Estimated effort:** 1-3 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### quickbuilder-suppression-helper-breaks-json-gate-contracts

- **Paths:** `scripts/forge/gates/_quickbuilder_guard.py`; `scripts/forge/gates/plan_structure_gate.py`; `scripts/forge/gates/merge_readiness.py`; `scripts/forge/gates/worktree_rules_gate.py`; `scripts/forge/gates/workboard_agent_claim.py`; `scripts/forge/gates/workboard_changed_files.py`; `scripts/forge/gates/workboard_claims.py`; `docs/QUICKBUILDER.md`
- **Evidence:** With QuickBuilder active in the live checkout, suppressible gates that advertise `--json` emit plain text because they call `announce_suppressed(...)` before parsing arguments. `python scripts/forge/gates/workboard_agent_claim.py --json` prints `Workboard agent claim gate: SKIP (QuickBuilder mode)`. `python scripts/forge/gates/workboard_changed_files.py --json` prints `Workboard changed files gate: SKIP (QuickBuilder mode)`. A combined probe of `python scripts/forge/gates/plan_structure_gate.py --json`, `python scripts/forge/gates/merge_readiness.py --json`, and `python scripts/forge/gates/worktree_rules_gate.py --json` prints `Plan structure gate: SKIP (QuickBuilder mode)`, `Merge readiness: SKIP (QuickBuilder mode)`, and `Worktree rules gate: SKIP (QuickBuilder mode)`. The shared helper is `scripts/forge/gates/_quickbuilder_guard.py:131-137`, where `announce_suppressed` only prints a text line and returns `True`.
- **Impact:** Automation that invokes suppressible gates with `--json` cannot parse results while QuickBuilder is active. This affects multiple coordination/location gates beyond the workboard-claims focused tests, and makes JSON mode unreliable exactly in the fast-build mode where agents are likely to use scripted commit helpers.
- **Suggested fix shape:** Make QuickBuilder suppression JSON-aware. Either parse `--json` before calling `announce_suppressed`, or extend the helper to accept an `as_json`/payload option and emit `{"gate": ..., "ok": true, "suppressed": true, "reason": "quickbuilder"}`. Add focused tests for at least two suppressible gates proving `--json` remains valid JSON when QuickBuilder is active, plus one human-mode test preserving the visible text skip line.
- **Risk:** Medium. QuickBuilder intentionally suppresses workflow gates, but changing helper output touches several commit-time gates; verify both human and machine-readable paths.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### workboard-claims-tests-and-json-mode-are-suppressed-by-quickbuilder

- **Paths:** `scripts/forge/gates/workboard_claims.py`; `tests/test_check_workboard_claims_gate.py`; `docs/QUICKBUILDER.md`
- **Evidence:** In this checkout, QuickBuilder is active. Running `python -m pytest tests/test_agent_preflight.py tests/test_check_workboard_claims_gate.py -q` reports `18 failed, 9 passed`, with every `tests/test_check_workboard_claims_gate.py` failure showing `Workboard claims gate: SKIP (QuickBuilder mode)` instead of exercising the workboard contract. The JSON tests fail at `json.loads(capsys.readouterr().out)` because `scripts/forge/gates/workboard_claims.py:752-761` calls `announce_suppressed("thomas-workboard-claims-gate", ROOT, "Workboard claims gate")` before argument parsing, so `--json` also returns plain text. `docs/QUICKBUILDER.md:57-59` says QuickBuilder relaxes claim/worktree gates, but the focused tests do not disable it or assert an opt-in suppression path.
- **Impact:** The workboard claims gate suite is non-hermetic and can go red or skip all meaningful validation on machines where QuickBuilder is active. The gate's `--json` contract is also violated under suppression, which can break automation expecting machine-readable gate results.
- **Suggested fix shape:** Default QuickBuilder off in `tests/test_check_workboard_claims_gate.py` via monkeypatch or fixture, then add separate QuickBuilder-active coverage. Move suppression handling after argument parsing or make `announce_suppressed` capable of emitting JSON when `--json` is requested, with payload fields such as `ok`, `gate`, `suppressed`, and `reason`.
- **Risk:** Medium. Workboard claim checks are core multi-agent coordination guardrails; fixes should preserve intentional QuickBuilder operator behavior while keeping tests and JSON automation deterministic.
- **Estimated effort:** 1-3 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### publish-tools-direct-commands-fail-and-use-wrong-root

- **Paths:** `scripts/forge/publish/preflight.py`; `scripts/forge/publish/snapshot.py`; `docs/GITHUB_PUBLISH_SAFETY_WORKFLOW.md`; `docs/THOMAS_BIBLE.md`; `tests/test_github_publish_preflight.py`; `tests/test_github_publish_snapshot.py`
- **Evidence:** The documented operator command in `docs/GITHUB_PUBLISH_SAFETY_WORKFLOW.md:76` is `python scripts/forge/publish/preflight.py --deep --strict`, but running `python scripts/forge/publish/preflight.py --json --strict` from the repo root fails immediately with `ModuleNotFoundError: No module named 'scripts.forge'` at `scripts/forge/publish/preflight.py:15`. Both publish scripts also define `ROOT = Path(__file__).resolve().parents[1]` (`preflight.py:28`, `snapshot.py:17`), which resolves to `C:\Users\corbe\Thomas\scripts\forge`, not the repo root. Import probing shows `(preflight.ROOT / ".gitignore").exists()` is false, `(preflight.ROOT / "docs" / "prod.toml").exists()` is false, and `snapshot.DEFAULT_REPO_HYGIENE_BASELINE` points at `C:\Users\corbe\Thomas\scripts\forge\docs\repo_hygiene_baseline.json`, which does not exist. `python -m pytest tests/test_github_publish_preflight.py tests/test_github_publish_snapshot.py -q` passes `18 passed`, so current focused coverage imports helpers but does not exercise the documented direct CLI invocation or root-derived default paths.
- **Impact:** The public publish safety workflow can fail before any secret/private-marker checks run. Even when imported, the publish snapshot/preflight defaults can resolve `.gitignore`, `docs/prod.toml`, and repo-hygiene baseline paths under `scripts/forge`, weakening or disabling checks that the Bible describes as restored publish guardrails.
- **Suggested fix shape:** Make both publish scripts bootstrap the repo root onto `sys.path` for direct execution, and correct their `ROOT` calculation to the actual repository root from `scripts/forge/publish/`. Add focused tests that invoke the documented command path via subprocess from repo root and assert `ROOT`-derived files resolve to the checked-in `.gitignore`, `docs/prod.toml`, and `docs/repo_hygiene_baseline.json`. Keep existing helper-level tests as regression coverage.
- **Risk:** Medium-high. These scripts are publish safety boundaries; a fix should be verified with focused publish preflight/snapshot tests and at least one direct CLI smoke test.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with publish-safety focused tests and no broad release-flow changes.

### workboard-problem-record-is-bytecode-placeholder

- **Paths:** `scripts/crew/workboard/problem_record.py`; `scripts/crew/brief/commit.py`; `scripts/auto_checks.py`; `scripts/doc.py`
- **Evidence:** `scripts/crew/workboard/problem_record.py:1-8` says it is recovered from bytecode cache, is a size-matched placeholder, and still needs real source restored or import/call sites replaced. `scripts/crew/brief/commit.py:90-91` includes `workboard_problem_record_smoke` as a local commit gate. `scripts/auto_checks.py` and `scripts/doc.py` also invoke `scripts/crew/workboard/problem_record.py` for problem recording. `python scripts/crew/workboard/problem_record.py --help` exits 0 with no help text, so the smoke gate can pass without proving behavior exists.
- **Impact:** A commit/local-check gate depends on a placeholder that can silently do nothing or rely on stale bytecode behavior. Failure recording may disappear exactly when automation needs it most.
- **Suggested fix shape:** Restore real source for `problem_record.py` from a trusted history/cache source or replace all call sites with a source-backed current module. Add a CLI smoke test that asserts meaningful `--help` output and a behavioral test that records a failure into the expected workboard/problem artifact.
- **Risk:** Medium-high. This is a gate/automation path, and changing it could affect commits and auto-check failure handling.
- **Estimated effort:** 0.5 day.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with tight tests and no broad workboard rewrites.

### commit-helper-suggests-deleted-agent-commit-path

- **Paths:** `scripts/crew/brief/commit.py`; `scripts/crew/brief/__init__.py`
- **Evidence:** `scripts/crew/brief/commit.py:479-484` builds fallback suggestions using `python scripts/agent_commit.py ...`; `scripts/crew/brief/commit.py:503-516` returns the same legacy path when agent identity is missing. `rg --files scripts | rg "agent_commit"` found no live `scripts/agent_commit.py`, and `python scripts/crew/brief/commit.py --help` shows the current entrypoint is `scripts/crew/brief/commit.py`.
- **Impact:** When commit scoping fails, Thomas tells agents to run a non-existent legacy command. That turns a recoverable claim/scope error into a dead-end workflow.
- **Suggested fix shape:** Update suggested commands to `python scripts/crew/brief/commit.py ...`, and adjust any rename note in `scripts/crew/brief/__init__.py` that still claims the legacy command lives at the old path. Add a focused assertion for suggested_command in the missing-agent and fallback-scope paths.
- **Risk:** Low. User-facing help text and tests only.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### renamed-workboard-scripts-still-documented-as-live

- **Paths:** `docs/THOMAS_BIBLE.md`; `AGENTS.md`; `scripts/crew/swarm/__init__.py`; `scripts/heartbeat.py`; `scripts/forge/gates/__init__.py`
- **Evidence:** `rg --files scripts | rg "workboard_worker|workboard_swarm|workboard_message|workboard_problem_record"` found no live legacy script files. Current docs still refer to legacy commands as live: `AGENTS.md:233` lists `workboard_task_manager.py`, `workboard_problem_record.py`, and `workboard_message.py`; `docs/THOMAS_BIBLE.md` references `scripts/workboard_worker.py`, `scripts/workboard_swarm.py`, and `agent_commit.py` in multiple operational sections. Some package docs intentionally mention renames, but several Bible entries describe old paths as real surveyed code.
- **Impact:** Agents following the source-of-truth docs can search or run deleted paths, misdiagnose workboard/swarm behavior, or update the wrong layer. This conflicts with the Bible rule that live code outranks stale docs and that stale docs should be corrected when drift is found.
- **Suggested fix shape:** Do a focused documentation drift pass that maps legacy paths to current `scripts/crew/*` and `scripts/forge/*` entrypoints, marking any historical references as historical. Add a docs grep test for forbidden live-command references if the repo already has a docs-contract test surface.
- **Risk:** Medium. `docs/THOMAS_BIBLE.md` is protected/source-of-truth text, so edits need careful scope and review.
- **Estimated effort:** 0.5 day.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if allowed to edit docs and protected Bible text.

### prompt-pack-p115-contract-test-is-hard-skipped

- **Paths:** `tests/prompt_pack/test_p115_plugin_gateway_handler_registry.py`; `thomas/plugins/p115_plugin_gateway_handler_registry.py`; `thomas/cli/commands/plugins/p115_plugin_gateway_handler_registry.py`; `scripts/xfail_scanner.py`; `scripts/forge/gates/xfail_growth_gate.py`
- **Evidence:** `tests/prompt_pack/test_p115_plugin_gateway_handler_registry.py:1-3` contains only `pytest.skip("p115 prompt-pack test payload was truncated in import bundle", allow_module_level=True)`. Running `python -m pytest tests/prompt_pack/test_p115_plugin_gateway_handler_registry.py -q` reports `1 skipped`, so the p115 prompt-pack contract currently contributes no assertions. Live implementation still exists at `thomas/plugins/p115_plugin_gateway_handler_registry.py:228-278` and `:415-435`, with a CLI wrapper at `thomas/cli/commands/plugins/p115_plugin_gateway_handler_registry.py:9-70`. The xfail debt machinery in `scripts/xfail_scanner.py` and `scripts/forge/gates/xfail_growth_gate.py` scans only `pytest.mark.xfail` markers, so this module-level skip is outside the visible xfail inventory/growth gate.
- **Impact:** A gateway/plugin registry surface can regress without prompt-pack coverage, and the skipped contract is not tracked by the same debt mechanism that prevents silent xfail growth. A future agent can see a green suite while the p115 contract is entirely absent.
- **Suggested fix shape:** Reconstruct the truncated p115 prompt-pack test payload from the implementation contract or remove the dead test file and replace it with focused assertions for config parsing, registry load failure modes, CLI JSON output, and plugin registration. Add a skip-inventory or skip-growth check for module-level `pytest.skip(..., allow_module_level=True)` so hard-disabled tests are tracked like xfails.
- **Risk:** Medium. Restoring tests may surface real implementation drift in a plugin/gateway path.
- **Estimated effort:** 0.5 day.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### placeholder-policy-misses-current-bytecode-placeholder-header

- **Paths:** `thomas/core/placeholder_policy.py`; `scripts/forge/gates/placeholder_completion_policy.py`; `tests/test_placeholder_completion_policy.py`; `scripts/crew/workboard/problem_record.py`
- **Evidence:** `scripts/crew/workboard/problem_record.py:1-8` is a current bytecode-backed placeholder with required `placeholder-*` fields and the line `This file is a size-matched placeholder; Python uses the cached .pyc`. `thomas/core/placeholder_policy.py:10-14` defines markers including `size-matched placeholder` and `python uses the cached .pyc`, but `is_placeholder_text` only scans the first six lines at `thomas/core/placeholder_policy.py:32-34`; the matching size/pyc marker is on line 8 in `problem_record.py`. Running `python scripts/forge/gates/placeholder_completion_policy.py --json` reports `"placeholder_file_count": 0`. Running `python -m pytest tests/test_placeholder_completion_policy.py -q` fails `test_placeholder_completion_policy_script_passes_on_repo` because `tests/test_placeholder_completion_policy.py:45-57` expects the repo gate to find at least one placeholder.
- **Impact:** The placeholder completion gate can pass while real placeholder-backed source files are present. That weakens the repo's explicit placeholder policy and leaves the focused regression test red.
- **Suggested fix shape:** Expand placeholder detection to scan enough header lines to include the required metadata block, or treat any file with multiple `placeholder-*` fields as placeholder-backed. Add a fixture matching `problem_record.py`'s `# Source: ... recovered from bytecode cache` header so the detector cannot regress to only `Source placeholder for ...`.
- **Risk:** Low-medium. The detector change is localized, but surfacing additional placeholders may make the gate fail and require either annotation fixes or deliberate cleanup.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### evolve-corpus-lock-hashes-are-stale

- **Paths:** `evolve_corpus/LOCK.json`; `evolve_corpus/cases/known_bad_print_only_repair.json`; `evolve_corpus/cases/known_good_minimal_session.json`; `evolve_supervisor/supervisor.py`; `tests/test_evolve_supervisor.py`
- **Evidence:** Running `python -m pytest tests/test_evolve_supervisor.py -q` currently times out after 30s but shows `1 failed, 46 passed`; the failing test is `tests/test_evolve_supervisor.py:1122-1128`, where `run_evolve_corpus(repo_root)` is expected to pass the locked seed corpus. The result fails closed with `corpus_hash_mismatch`. `evolve_supervisor/supervisor.py:226-258` rejects any locked corpus file whose SHA-256 differs from `evolve_corpus/LOCK.json`. The lock expects `cases/known_bad_print_only_repair.json` hash `b1322b0f20dfbb3e2c375b771bce3f0da0d0e42e1d3fccf38492319eb6dd21ec`, but the checked-in file hashes to `fe91d17fd688048bdde7464394f63debe8629762fd3c1dc0050e10092eadfffa`. The lock expects `cases/known_good_minimal_session.json` hash `5db526611f7a91608d4a44423a70e32d50d0868dd204b5968a37b8d9bd95e9f5`, but the checked-in file hashes to `f6a8beb65896ef2bcab15e660e8f79fd39dc325ae82760283ff6bba60dafb50d`.
- **Impact:** The evolve supervisor's frozen corpus verification is red in the live repo, so supervisor regression coverage is partially blocked and any corpus-backed confidence claims are stale until either the lock or the case files are reconciled.
- **Suggested fix shape:** Review the two changed corpus case files for intended content. If the content is correct, regenerate/update `evolve_corpus/LOCK.json` through the approved corpus-lock workflow and add a regression asserting the lockfile and case set stay synchronized. If the content is unintended drift, restore the case files to the locked hashes.
- **Risk:** Medium. The lock is a safety boundary for evolve-supervisor promotion behavior, so updating it should require review of case semantics rather than a blind hash bump.
- **Estimated effort:** 1-3 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to corpus-lock reconciliation and focused supervisor tests.

### code-intake-forge-tools-use-scripts-forge-as-repo-root

- **Paths:** `scripts/forge/intake/seed_batch.py`; `scripts/forge/intake/cli.py`; `docs/CODE_INTAKE_PIPELINE.md`; `tests/test_code_intake_seed_batch.py`
- **Evidence:** `scripts/forge/intake/seed_batch.py:14-15` and `scripts/forge/intake/cli.py:24-25` both return `Path(__file__).resolve().parents[1]` as the repo root, which resolves to `C:\Users\corbe\Thomas\scripts\forge` for files under `scripts/forge/intake/`. The documented command `python scripts/forge/intake/seed_batch.py --batch-id B01` from `docs/CODE_INTAKE_PIPELINE.md:34` fails with `error: index not found: C:\Users\corbe\Thomas\scripts\forge\docs\REFERENCE_CLI_CATCHUP_PROMPT_BATCH_INDEX_216_2026-02-20.csv`, and it creates an untracked `scripts/forge/code_intake/` layout before the index check because `_ensure_layout(root)` runs at `scripts/forge/intake/seed_batch.py:168-169`. `python scripts/forge/intake/cli.py status` reports `intake_root: C:\Users\corbe\Thomas\scripts\forge\code_intake` instead of the checked-in repo-root `code_intake/`. Existing `tests/test_code_intake_seed_batch.py` covers `_domain_allowed_paths`, but its historical B01 loader assertion is skipped at `tests/test_code_intake_seed_batch.py:26-33`, so the default command path is not guarded.
- **Impact:** The public code-intake workflow documented for high-volume prompt drops is broken by default and can silently create queue state under `scripts/forge/` instead of the repo-root `code_intake/` tree. Agents following the docs may fail to seed batches or validate/apply against the wrong intake directory.
- **Suggested fix shape:** Correct both `_repo_root()` helpers to resolve the actual repo root from the nested `scripts/forge/intake/` path, then add focused tests for `seed_batch.py --batch-id ... --dry-run` and `cli.py status/init` using a temp root or fixture index. Move `_ensure_layout(root)` after index validation in `seed_batch.py` or make dry-run/index failures side-effect free. Replace the skipped B01 assertion with a non-competitor fixture so the loader path remains covered.
- **Risk:** Medium. The fix is localized, but it changes default filesystem targets for intake commands and should verify that no legitimate workflow depends on the accidental `scripts/forge/code_intake` location.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes.

### repo-orphan-inventory-snapshot-is-stale-and-ungated

- **Paths:** `docs/ops/repo_orphan_inventory.md`; `docs/ops/repo_orphan_inventory.json`; `scripts/repo_orphan_inventory.py`; `tests/test_repo_orphan_inventory.py`
- **Evidence:** The checked-in markdown snapshot says `generated_at_utc: 2026-02-25T22:09:59.135530+00:00`, `inventory_count: 468`, and `untracked_count: 468` at `docs/ops/repo_orphan_inventory.md:3-8`. It also lists tracked/current paths such as `docs/ops/repo_orphan_inventory.md`, `scripts/a.py`, and `tests/test_imgproc_panorama.py` as `untracked_review` at `docs/ops/repo_orphan_inventory.md:41`, `:100`, and `:162`. Live Git disagrees: `git ls-files docs/ops/repo_orphan_inventory.md scripts/a.py tests/test_imgproc_panorama.py` prints all three paths. Running the generator without writing (`python scripts/repo_orphan_inventory.py --json`) reports `live_inventory_count=7`, `live_untracked_count=4`, and none of those three paths appear in the live `entries`. `python -m pytest tests/test_repo_orphan_inventory.py -q` passes, but the tests mock `build_inventory` and verify rendering behavior; they do not fail when the committed inventory snapshot is stale.
- **Impact:** The repo cleanup/orphan inventory is presented as an operational artifact, but it is months out of date and actively misclassifies tracked files as untracked review debt. Agents using it can waste cleanup time on already-landed files or distrust the generated hygiene workflow because the checked-in evidence contradicts Git.
- **Suggested fix shape:** Decide whether the inventory outputs are meant to be tracked snapshots or generated local artifacts. If tracked, regenerate `docs/ops/repo_orphan_inventory.md` and `docs/ops/repo_orphan_inventory.json` from the current repo and add a freshness/check mode that compares generated payload counts and entries against the committed files. If local-only, move the outputs to ignored/generated locations and update docs/tests so stale snapshots cannot masquerade as current cleanup guidance.
- **Risk:** Low-medium. Regenerating or untracking generated hygiene artifacts is mostly docs/automation work, but it changes cleanup guidance and may expose real current orphan debt.
- **Estimated effort:** 1-3 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow docs/automation claim and a freshness-focused test.

### bible-status-rollup-depends-on-deleted-subcheck-scripts

- **Paths:** `scripts/bible_status.py`; `docs/THOMAS_BIBLE.md`; `docs/PIPELINE_PRACTICES.md`; `plans/thomas/problems/praxis-unbypassable-2026-05-29/SCOREBOARD.md`
- **Evidence:** `scripts/bible_status.py:102-104` shells out to `scripts/bible_lint.py`, `scripts/bible_freshness.py`, and `scripts/bible_coverage.py` with `--json`. Live repo evidence disagrees with that dependency contract: `git ls-files scripts/bible_lint.py scripts/bible_freshness.py scripts/bible_coverage.py scripts/bible_status.py` returns only `scripts/bible_status.py`, and `Test-Path` returns false for all three subcheck scripts. Running `python scripts/bible_status.py --json` exits 1 with `status: LINT_BROKEN`, `sections: 0`, `coverage_pct: 0.0`, and `subcheck_errors` of `bible_lint: no parseable JSON output`, `bible_freshness: no parseable JSON output`, and `bible_coverage: no parseable JSON output`. `python scripts/bible_status.py --detail` prints the same zero-section health view plus `lint check did not return parseable output`. `docs/PIPELINE_PRACTICES.md:82` still presents `python scripts/bible_status.py` as repo-quality health, and `plans/thomas/problems/praxis-unbypassable-2026-05-29/SCOREBOARD.md:742-747` marks the prior B8 crash surface closed while noting the same missing-subcheck strings. There is no focused `tests/test_bible_status.py`, and `rg --files tests | rg "bible_status|bible_lint|bible_freshness|bible_coverage"` finds no test file for this rollup.
- **Impact:** The session-start Bible health command always reports the Bible as broken with zero parseable sections in the live repo. That makes a core source-of-truth health signal unactionable, hides real freshness/coverage status behind missing-script errors, and leaves agents unable to distinguish actual Bible drift from deleted-tool wiring.
- **Suggested fix shape:** Rewire `bible_status.py` to call the current Bible lint/freshness/coverage implementation if it exists, restore the three subcheck scripts as thin wrappers, or retire the rollup if the Bible health model moved elsewhere. Add focused tests that simulate missing subchecks and a healthy subcheck path, asserting that JSON includes actionable failed command details and that the live repo returns nonzero section/coverage counts when the Bible is parseable. Update docs/problem-board text so `bible_status.py` is not advertised as repo-quality health until the current implementation works.
- **Risk:** Medium. The fix touches a source-of-truth health command and may reveal real Bible lint/freshness failures once the wiring is restored.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a docs/tooling-only claim and focused tests around `scripts/bible_status.py`.

### worktree-debt-remediation-points-to-missing-consolidate-command

- **Paths:** `scripts/crew/worktree_debt.py`; `scripts/forge/gates/worktree_creation_gate.py`; `tests/test_worktree_governance.py`; `thomas/cli/main.py`; `thomas/cli/STATUS.md`
- **Evidence:** The live worktree debt detector is currently over the configured ceiling: `python scripts/crew/worktree_debt.py --summary` exits 1 and prints `worktrees: 9 total, 6 dirty, 4 stale (ceiling=5)`. The full report recommends `Recommended: run \`thomas consolidate\` to merge or retire stale/dirty worktrees.` from `scripts/crew/worktree_debt.py:45` and `:90`. The creation gate gives the same remediation text at `scripts/forge/gates/worktree_creation_gate.py:145` when the dirty-worktree ceiling blocks a new worktree. But `python -m thomas.cli.main consolidate --help` fails with `Error: No such command 'consolidate'`, and `python -m thomas.cli.main --help` only lists nearby commands such as `repo-clean` and `ship`, not `consolidate`. `python -m pytest tests/test_worktree_governance.py -q` passes because it asserts the detector returns `MERGE DEBT` and exit code 1, but it does not assert that the recommended remediation command is implemented or routed.
- **Impact:** When Thomas correctly blocks additional worktree sprawl, it sends agents to a missing remediation command. That turns a recoverable merge-debt alarm into a dead-end operator workflow and makes repeated dirty worktree buildup harder to resolve from the startup/gate guidance.
- **Suggested fix shape:** Either implement a real `thomas consolidate` CLI that wraps the supported worktree ledger/debt cleanup flow, or change both remediation strings to existing commands with clear manual steps. Add a focused test that every command mentioned in worktree debt and creation-gate remediation is either present in `thomas.cli.main` or explicitly documented as future/not-yet-implemented. If a full consolidate command is added, cover at least dry-run/list behavior before any destructive cleanup.
- **Risk:** Medium. Text-only remediation changes are low risk, but implementing consolidation could touch worktree cleanup behavior and must avoid deleting or moving user work without explicit approval.
- **Estimated effort:** 2-6 hours depending on whether the fix is command-routing text or a real dry-run-first consolidation CLI.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to non-destructive CLI routing/help text and focused tests; destructive cleanup behavior should require human review.

### agent-file-editing-rules-mislabels-task-manager-as-monolith-stub

- **Paths:** `docs/AGENT_FILE_EDITING_RULES.md`; `scripts/crew/tasks/manager.py`; `scripts/crew/tasks/*`; `tests`
- **Evidence:** `docs/AGENT_FILE_EDITING_RULES.md:105-112` lists `scripts/crew/tasks/manager.py` under "Monolith-loaded files (edit the PARTS, not the stub)" and says it is a `STUB` that loads `workboard_task_manager_part01/02/03/04.py`. Live repo evidence disagrees: `scripts/crew/tasks/manager.py:1-55` is a real source module with imports from `scripts.crew.tasks.base`, `messages`, `plans`, `reactivate`, and `sessions`, and `(Get-Item scripts/crew/tasks/manager.py).Length` reports `28532` bytes. `rg --files scripts | rg "workboard_task_manager_part"` returns no part files. `rg -n "workboard_task_manager_part|scripts/crew/tasks/manager.py.*STUB"` finds only this stale docs line, and there is no focused `tests/test_agent_file_editing_rules.py` guarding that the editing rules match the current source layout.
- **Impact:** Agents following the editing rules can avoid the actual task-manager source, search for deleted part files, or make changes in the wrong layer. That is especially risky because `scripts/crew/tasks/manager.py` is used by workboard automation and is a common remediation command printed by bootstrap and gate flows.
- **Suggested fix shape:** Update `docs/AGENT_FILE_EDITING_RULES.md` to classify `scripts/crew/tasks/manager.py` as source-backed current code, with the current helper modules under `scripts/crew/tasks/` as its real decomposition. Add a lightweight docs consistency test or grep gate for documented monolith part files so deleted `*_partNN.py` references cannot be advertised as live edit targets.
- **Risk:** Low. This is documentation and optional docs-test work, but it affects where future agents make workboard automation edits.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with docs-only scope plus a focused consistency test if desired.

### loop-and-chat-docs-point-at-deleted-monolith-part-files

- **Paths:** `docs/AGENT_FILE_EDITING_RULES.md`; `thomas/agent/README.md`; `thomas/server/README.md`; `thomas/server/routes/README.md`; `thomas/chat/README.md`; `thomas/server/web/README.md`; `thomas/tools/file_readers.py`; `thomas/agent/loop.py`; `thomas/server/routes/chat_aiohttp.py`
- **Evidence:** Multiple current docs still tell agents to edit deleted monolith part files. `docs/AGENT_FILE_EDITING_RULES.md:107-108` says `thomas/agent/loop.py` loads `loop_part01/02/03.py` and `thomas/server/routes/chat_aiohttp.py` loads `chat_aiohttp_part01/02/03.py`; `docs/AGENT_FILE_EDITING_RULES.md:181-182` repeats those paths in the architecture quick reference. `thomas/agent/README.md:19-45` says `loop.py` is a loader stub assembled from `loop_part01.py`, `loop_part02.py`, and `loop_part03.py`. `thomas/server/routes/README.md:23` and `:102-104`, `thomas/server/README.md:109`, `thomas/chat/README.md:208`, `:287`, and `:392`, `thomas/server/web/README.md:310`, and `thomas/tools/file_readers.py:10` all point at `chat_aiohttp_part02.py` or sibling parts as live edit/runtime locations. Live repo evidence disagrees: `rg --files thomas | rg "(loop_part0[123]|chat_aiohttp_part0[123])\\.py$"` returns no files. `thomas/agent/loop.py` is an 11164-byte source-backed facade that imports `loop_core`, `loop_execution`, `loop_planning`, and related modules; `thomas/server/routes/chat_aiohttp.py` is a 1274-byte compatibility shim importing `chat_aiohttp_handlers` and `chat_aiohttp_helpers`.
- **Impact:** Agents following the docs can spend time searching for deleted part files, avoid editing the current source-backed modules, or patch the wrong chat/agent layer. This is a high-friction stale-doc problem on core agent-loop and chat-route surfaces.
- **Suggested fix shape:** Update the affected README/editing-rule references to the current decomposition: agent loop work lives in `loop_core`, `loop_execution`, `loop_helpers`, `loop_planning`, `loop_streaming`, and related modules; chat route work lives in `chat_aiohttp_handlers`, `chat_aiohttp_helpers`, and the current compatibility shim. Add a lightweight docs consistency check for referenced `*_partNN.py` files so docs cannot advertise deleted monolith parts as live edit targets.
- **Risk:** Low-medium. The work is documentation and optional docs-test coverage, but it spans several source-of-truth README files and should avoid erasing genuinely historical notes without marking them historical.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with docs-only scope and focused grep/test verification.

### build-module-registry-help-writes-untracked-registry

- **Paths:** `scripts/build_module_registry.py`; `MODULE_REGISTRY.md`; `scripts/forge/gates/public_repo_leak_guard.py`
- **Evidence:** `scripts/build_module_registry.py` does not parse arguments; its `if __name__ == "__main__": main()` path always scans modules and writes `OUTPUT = REPO_ROOT / "MODULE_REGISTRY.md"` at `scripts/build_module_registry.py:24`, then `OUTPUT.write_text(...)` at `scripts/build_module_registry.py:142`. Running the expected help probe `python scripts/build_module_registry.py --help` exits 0 but prints `Wrote C:\Users\corbe\Thomas\MODULE_REGISTRY.md (174 modules, 33 with STATUS.md)` instead of usage text, and creates an untracked 15180-byte `MODULE_REGISTRY.md`. `git ls-files MODULE_REGISTRY.md` returns no tracked file. `scripts/forge/gates/public_repo_leak_guard.py:63` explicitly includes `MODULE_REGISTRY.md` in its private marker/path list, so a mistaken help invocation creates a root-level artifact that publish/leak tooling already treats as sensitive/local.
- **Impact:** A harmless-looking `--help` or argument typo mutates the worktree and creates an untracked root artifact. Agents doing command discovery can accidentally dirty the repo or trip publish/leak hygiene on a file they did not intend to generate.
- **Suggested fix shape:** Add `argparse` with `--help`, `--output`, and a clear `--write` or default write mode, and make unknown arguments fail without writing. Consider moving the default output under `docs/ops/` or marking the command as intentionally write-only in docs. Add a focused subprocess test asserting `python scripts/build_module_registry.py --help` returns 0, prints usage, and leaves `MODULE_REGISTRY.md` absent.
- **Risk:** Low. The fix is localized CLI behavior, but any change to default generation path should respect existing local/operator workflows.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow script/test/docs claim and no changes to generated registry contents unless explicitly requested.

### feature-master-sync-check-fails-on-date-only-regeneration

- **Paths:** `scripts/sync_feature_master_list.py`; `docs/FEATURE_MASTER_LIST.md`; `docs/feature_master_manifest.json`; `tests/test_sync_feature_master_list.py`; `tests/test_feature_master_sync.py`; `scripts/auto_checks.py`
- **Evidence:** `python scripts/sync_feature_master_list.py --check --json` exits 1 with `"stale": true`, `"error": "docs/FEATURE_MASTER_LIST.md is stale"`, and totals `{"DONE": 26, "INBOX": 0, "MISSING": 4}`. Rebuilding in memory from `docs/feature_master_manifest.json` produces the same length and same totals as the checked-in `docs/FEATURE_MASTER_LIST.md`; the first and only observed content difference is the generated date line: current `docs/FEATURE_MASTER_LIST.md` has `**Last Updated:** 2026-06-14`, while `build_document(...)` emits `**Last Updated:** 2026-06-26`. `scripts/sync_feature_master_list.py:217-231` uses `datetime.now(timezone.utc).date().isoformat()` when no `date_stamp` is passed, so check mode becomes stale each day even when feature content is unchanged. `scripts/auto_checks.py:70` includes this gate. `python -m pytest tests/test_sync_feature_master_list.py tests/test_feature_master_sync.py -q` passes 8 tests, but the focused tests do not cover the live-repo date-only staleness behavior.
- **Impact:** A generated-doc freshness gate can fail solely because time passed, not because the manifest or feature statuses changed. That makes auto-checks noisy and encourages unnecessary regeneration commits that only bump `Last Updated`.
- **Suggested fix shape:** Make check mode compare semantic feature content separately from the volatile date stamp, or preserve the existing date when generated content is otherwise identical. Add a regression where a master list with an older `Last Updated` but identical generated tables passes `--check`, while real manifest/status drift still fails.
- **Risk:** Low-medium. The change is localized to a generated-doc gate, but it should preserve intentional regeneration behavior when content actually changes.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a focused script/test/docs claim.

### verification-record-gate-accepts-skipped-regression-as-passing

- **Paths:** `scripts/create_verification_record.py`; `scripts/forge/gates/verification_record_gate.py`; `.git/agent_verification.json`; `tests`
- **Evidence:** `scripts/create_verification_record.py:107` exposes `--skip-regression` and `scripts/create_verification_record.py:130-156` leaves `regression_passed = True` while recording `regression_check.command` as `"skipped"` when that flag is used. A local probe using the helper produced a clean record with `"regression_check": {"command": "skipped", "passed": true, "output_hash": ""}` and printed `Record is clean. You can now commit your changes.` The gate then accepts that shape because `scripts/forge/gates/verification_record_gate.py:103-117` only checks that `regression_check` exists and that `passed` is truthy; it does not reject `command == "skipped"` or an empty output hash. A direct function probe of `_check_tests_run` and `_check_regression` on a skipped-regression record returned `tests_errors=[]` and `regression_errors=[]`. `rg -n "skip-regression|_check_regression|create_verification_record" tests ...` found no focused test for skipped-regression enforcement.
- **Impact:** Once enough code files are staged to require a verification record, an agent can intentionally skip the regression command and still produce a record that the gate treats as passing. That weakens the verification protocol's claim that existing regression tests passed before commit.
- **Suggested fix shape:** Decide whether `--skip-regression` should be forbidden for gated commits or allowed only with an explicit justification/breakglass field. Update `create_verification_record.py` to record skipped regression as non-passing unless justified, and update `verification_record_gate.py` to reject `regression_check.command == "skipped"` or empty regression evidence by default. Add focused tests for skipped regression records, justified exceptions if supported, and normal passing regression records.
- **Risk:** Medium. Tightening this gate may block workflows that have relied on `--skip-regression`, so rollout should provide clear remediation text.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with focused verification-helper/gate tests and no broad commit-helper changes.

### verification-record-gate-accepts-zero-test-non-pytest-command

- **Paths:** `scripts/create_verification_record.py`; `scripts/forge/gates/verification_record_gate.py`; `.git/agent_verification.json`; `tests`
- **Evidence:** `scripts/create_verification_record.py:103` describes `--test-command` as "The pytest command to run for your changes", but `_run_command` at `scripts/create_verification_record.py:39-57` executes any shell string and `_parse_pytest_output` at `scripts/create_verification_record.py:67-80` returns `(0, 0)` when output contains no pytest summary. `_check_tests_run` in `scripts/forge/gates/verification_record_gate.py:77-101` accepts that shape because it only rejects negative pass/fail counts and positive failures. A direct function probe returned `_check_tests_run({"tests_run": {"command": "python -c \"print(123)\"", "passed": 0, "failed": 0, "exit_code": 0}}) == []`, the same as a nominal pytest record. A helper probe using `--test-command "python -c \"print(123)\""` printed `0 passed, 0 failed (exit code 0)` and `Record is clean. You can now commit your changes.` `rg -n "tests_run|_check_tests_run|parse_pytest|0 passed|test-command"` found no focused test requiring the command to be pytest-like or to report at least one executed test.
- **Impact:** A gated commit can satisfy the verification-record "tests were run" requirement with any successful shell command that emits no pytest summary. That lets non-test smoke commands or inert commands masquerade as test evidence.
- **Suggested fix shape:** Require `tests_run.command` to be a recognized test command or require evidence of at least one executed test unless an explicit non-pytest verifier mode is declared. Update `create_verification_record.py` to mark zero-test output as non-clean by default, and update `verification_record_gate.py` to reject `passed == 0` and `failed == 0` records unless a structured verifier type/justification is present. Add focused tests for zero-test output, real pytest pass output, and any supported non-pytest verifier escape hatch.
- **Risk:** Medium. Tightening this could affect workflows that use custom non-pytest verification commands, so the remediation should define a supported structured path for those commands.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with focused verification-helper/gate tests and clear compatibility handling for non-pytest verifiers.

### feature-catalog-gate-ignores-cli-arguments

- **Paths:** `scripts/forge/gates/feature_catalog_gate.py`; `docs/FEATURE_CATALOG.md`; `scripts/doc.py`; `tests`
- **Evidence:** `scripts/forge/gates/feature_catalog_gate.py` has no argument parser; its entrypoint is only `raise SystemExit(run())`, and `run()` always validates the live `docs/FEATURE_CATALOG.md`. Running `python scripts/forge/gates/feature_catalog_gate.py --help` exits 0 but prints the normal gate result (`Feature catalog gate: OK ... documented feature IDs: 29`) instead of usage. Running `python scripts/forge/gates/feature_catalog_gate.py --json` also prints human text, and running `python scripts/forge/gates/feature_catalog_gate.py --bad-flag` still exits 0 with the same PASS output. `scripts/doc.py:55` includes this gate in the docs validation pipeline, but `rg -n "feature_catalog_gate|Feature catalog gate|FEATURE_CATALOG" tests ...` found no focused test file covering help, JSON, or unknown-argument behavior.
- **Impact:** Command discovery and automation can get false confidence from typoed feature-catalog gate invocations because unknown flags are silently ignored. The gate also lacks machine-readable output while most neighboring gate scripts support `--json`, making it harder to include in structured docs/release automation.
- **Suggested fix shape:** Add `argparse` support with `--json`, normal `--help`, and default rejection of unknown arguments. Return a JSON payload with `ok`, `missing_headers`, `missing_feature_ids`, `duplicate_feature_ids`, and `documented_feature_count`. Add focused subprocess or module tests for `--help`, `--json`, a known-pass catalog, a fixture with missing IDs, and an unknown flag failing without running as a pass.
- **Risk:** Low. The fix is localized to a gate CLI, but callers that scrape the current human text should keep working in default text mode.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow gate/test claim.

### worktree-rules-gate-pass-message-claims-deleted-file-was-checked

- **Paths:** `scripts/forge/gates/worktree_rules_gate.py`; `tests/test_check_worktree_rules_gate.py`; `docs/THOMAS_BIBLE.md`; `WORKTREE_RULES.md`
- **Evidence:** `WORKTREE_RULES.md` is absent from the live checkout and `git ls-files WORKTREE_RULES.md` returns no tracked file. The gate source explicitly documents that absence as intentional: `scripts/forge/gates/worktree_rules_gate.py:89-94` says `WORKTREE_RULES.md` was deleted in the 2026-05-21 pre-public cleanup and should be skipped silently when absent. But the success path still prints `- AGENTS.md and WORKTREE_RULES.md contain required worktree policy text` at `scripts/forge/gates/worktree_rules_gate.py:111-112`. A direct in-process probe with QuickBuilder suppression stubbed out returned `rc=0` and that PASS text while `worktree_rules_exists=False` and `git_tracked=False`. The focused tests only assert a present-fixture PASS string and missing-policy failure; there is no test for the intentional absent-file pass text, and in the current checkout `python -m pytest tests/test_check_worktree_rules_gate.py -q --tb=short` is also suppressed by QuickBuilder before exercising those assertions.
- **Impact:** Agents and operators can read a successful gate result as proof that the deleted `WORKTREE_RULES.md` file was present and validated. That weakens confidence in worktree-discipline diagnostics and makes stale-file references harder to notice during onboarding or gate debugging.
- **Suggested fix shape:** Split the success message by actual state: when `WORKTREE_RULES.md` is absent, report that AGENTS policy passed and the legacy rules file is absent/optional; when present, report that both files were checked. Add focused tests with QuickBuilder disabled/stubbed for both present and absent `--rules` paths, plus a guard that the absent-file PASS output does not claim the file contains required text.
- **Risk:** Low. This is a gate-message/test correction; avoid making the absent legacy file required again.
- **Estimated effort:** 1 hour.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow gate/test claim.

### doc-reliability-runner-references-deleted-gates-and-test

- **Paths:** `scripts/doc.py`; `scripts/forge/gates/competitive_scope_gate.py`; `scripts/forge/gates/reference_cli_metric_parity_gate.py`; `tests/test_reference_cli_metric_parity_gate.py`; `docs/PROJECT_SCOPE.md`; `docs/launch/LAUNCH_GATE_SCOREBOARD_2026-02-25.md`
- **Evidence:** `scripts/doc.py:56-57` still includes `Competitive scope gate` and `Reference CLI metric parity gate` commands pointing at `scripts/forge/gates/competitive_scope_gate.py` and `scripts/forge/gates/reference_cli_metric_parity_gate.py`. `scripts/doc.py:74` also includes `tests/test_reference_cli_metric_parity_gate.py` in its critical test list. Live path checks show all three are missing and untracked: `scripts/forge/gates/competitive_scope_gate.py exists=False tracked=False`, `scripts/forge/gates/reference_cli_metric_parity_gate.py exists=False tracked=False`, and `tests/test_reference_cli_metric_parity_gate.py exists=False tracked=False`. Direct probes fail with Python `can't open file ... [Errno 2] No such file or directory`. `git log --all --` for those paths points back to the 2026-05 pre-public cleanup/rename history. `docs/PROJECT_SCOPE.md:82-83` still names the deleted forge gate paths and `docs/launch/LAUNCH_GATE_SCOREBOARD_2026-02-25.md:13-14` still names the older `scripts/check_*` forms.
- **Impact:** The central `python scripts/doc.py` reliability runner can fail on deleted commands before it proves the current docs/protocol safety suite. Operators following `docs/ops/GATEWAY_SECURITY_RUNBOOK.md` or onboarding notes get a red doc runner from stale wiring rather than current gate failures, and the critical-test list advertises coverage that cannot run.
- **Suggested fix shape:** Remove or replace the deleted competitive/reference parity steps in `scripts/doc.py` with current live gates or a documented retired-check note. Add a focused test that every `GATE_COMMANDS` script and every `CRITICAL_TEST_FILES` entry exists, or explicitly allowlisted retired entries are skipped with clear messaging. Update `docs/PROJECT_SCOPE.md` and any launch scoreboard references to mark the old checks historical or route to the current replacement.
- **Risk:** Low-medium. The fix is mostly runner/docs wiring, but removing retired parity gates should be explicit so no intended launch-blocking check disappears silently.
- **Estimated effort:** 1-3 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow docs-runner/test/docs claim.

### auto-checks-quick-passes-with-invalid-ruff-noqa-warnings

- **Paths:** `scripts/auto_checks.py`; `thomas/cli/commands/channel_ops/p096_channel_integration_into_existing_channels_module.py`; `thomas/cli/commands/plugins/p097_plugin_package_bootstrap.py`; `tests/test_ruff_config.py`; `tests`
- **Evidence:** Running `python scripts/auto_checks.py --quick --no-record-problem-on-fail` exits 0 and reports both quick steps PASS, but Ruff also emits warnings: `Invalid rule code provided to # noqa ... WPS430` at `thomas/cli/commands/channel_ops/p096_channel_integration_into_existing_channels_module.py:71`, `:82`, and `thomas/cli/commands/plugins/p097_plugin_package_bootstrap.py:28`. The focused file probe `python -m ruff check ... --select F821,F822,F823,F632,E902` exits 0 with `All checks passed!`, so the stale suppressions are warning-only under the fatal-lint selection. `rg -n "WPS430|Invalid rule code|noqa.*WPS"` finds only those three inline suppressions and no queue entry. `tests/test_ruff_config.py` checks removed rule IDs in `pyproject.toml` ignore configuration, but there is no focused test/gate covering invalid inline `# noqa` rule codes in source files.
- **Impact:** Thomas' advertised quick verification path can look green while still printing lint-configuration warnings. That trains agents to ignore noisy automation output and leaves stale suppressions from a different lint plugin in production CLI modules.
- **Suggested fix shape:** Remove the obsolete `WPS430` inline suppressions or replace them with Ruff-recognized suppressions only if a current Ruff rule actually fires. Add a lightweight source scan or Ruff-config test that fails on invalid inline `# noqa` rule codes, so future plugin-specific suppressions do not silently rot in the quick gate output.
- **Risk:** Low. This is a small lint-hygiene cleanup; verify that removing the comments does not expose a current lint failure under the supported Ruff rules.
- **Estimated effort:** 1 hour.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow lint/test claim.

### chat-shell-root-route-depends-on-untracked-asset-and-untested-handler

- **Paths:** `thomas/server/app_middleware_helpers.py`; `thomas/server/app_middleware_handlers.py`; `thomas/server/app_routes_init.py`; `thomas/server/routes/core_aiohttp.py`; `thomas/server/web/chat.html`; `thomas/server/web/index.html`; `tests/test_server_app_routes_init.py`; `docs/THOMAS_BIBLE.md`; `docs/FEATURE_CATALOG.md`
- **Evidence:** The live worktree now has tracked handler changes that route `/` through `build_page_handlers(...).index`, and `app_middleware_helpers.py` has been changed so `index()` reads and returns `web_dir / "chat.html"` while the legacy `index.html` moved behind a new `classic()` handler. `git status --short` shows the route/helper files modified but `thomas/server/web/chat.html` is untracked. If those tracked handler changes are committed without the untracked asset, the fallback path still returns `web.FileResponse(web_dir / "chat.html")`, so the root page can fail at runtime. Focused route coverage (`python -m pytest tests/test_server_app_routes_init.py tests/test_deliverable_ranking.py -q --tb=short`) passes 23 tests, but `tests/test_server_app_routes_init.py` builds `locals_dict["index"] = _simple_page` and therefore bypasses `build_page_handlers()` and does not prove `/` serves an existing `chat.html` or `/classic` serves `index.html`. Docs still describe the older surface: `docs/THOMAS_BIBLE.md:670` says the aiohttp app serves `web/index.html` at `/`, `docs/THOMAS_BIBLE.md:845` says `thomas/server/web/index.html` is static HTML served at `/` and `/mission`, and `docs/FEATURE_CATALOG.md:18` lists `thomas/server/web/index.html` plus `js/chat.js`/`js/app.js` for `ui.web_chat`.
- **Impact:** The in-progress chat shell can merge with a missing root-page asset or without tests proving the new `/` and `/classic` contract. The documentation mismatch also sends future agents to `index.html` as the canonical chat surface after the code has moved the root route to `chat.html`.
- **Suggested fix shape:** Before merging the chat shell lane, ensure `thomas/server/web/chat.html` and its required CSS/JS assets are tracked, add a focused route test that uses real `build_page_handlers()` against a fixture web directory and asserts `/` reads `chat.html` while `/classic` reads `index.html`, and update Bible/feature catalog/server docs to distinguish the new chat shell from the legacy SPA. Consider a startup/static asset check that fails if a page handler points at a missing HTML file.
- **Risk:** Medium. The fix touches the root web UX and docs, but the safe path is to add focused tests and track the asset before changing more routing behavior.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to test/docs/asset-wiring verification around the existing dirty chat-shell lane; broader UI design should stay with the lane owner.

### active-folder-auto-claims-refresh-dead-process-owners

- **Paths:** `scripts/active_folders.py`; `runtime/coordination/active_folders.json`; `scripts/crew/workboard/claim.py`; `scripts/crew/workboard/message.py`; `thomas/forge/anvil/native_orchestration.py`; `tests/test_native_orchestration.py`; `plans/thomas/WORKBOARD.md`
- **Evidence:** `python scripts/crew/workboard/claim.py --list --json` reported `active_claim_count: 0`, while `python scripts/active_folders.py list --json` reported four unexpired `pre-commit staged auto-claim` leases with empty `session_id`, including `thomas-codebase-issue-researcher` on `plans/thomas/THOMAS_CODEBASE_ISSUE_QUEUE.md`. A PID liveness probe against that active-folder roster showed all four owners dead (`codex-codebase-issue-ranker` pid 43776, `thomas-agentic-ai-repo-ranker` pid 53704, `thomas-agentic-ai-repo-researcher` pid 55300, and `thomas-codebase-issue-researcher` pid 33940). `plans/thomas/WORKBOARD.md:363-370` records repeated blocker updates from 2026-06-26T18:20Z through 19:57Z where the same four automation lanes keep receiving refreshed unexpired active-folder claims whose Windows PIDs are not alive, plus coordination messages addressed to dead `process:<pid>` owners. `scripts/active_folders.py` prunes expired claims in `_list`, but `_guard_staged` creates auto-claims and does not expose owner liveness or reconcile those temporary guard claims with workboard claim release. The dirty native-orchestration lane has tests that surface `dead_owner_active_folder_claims` and recommend cleanup, but the active-folder guard itself still leaves refreshed dead-owner leases visible to dispatch.
- **Impact:** `claim.py` and `active_folders.py` can disagree about ownership: a worker can release its workboard claim while a dead-process active-folder auto-claim still blocks or confuses coordination until TTL expiry, then the next heartbeat/commit cycle recreates the problem. Native orchestration gets noisy blocker churn and false overlap signals instead of a reliable "live owner" contract for queue/ranking files.
- **Suggested fix shape:** Add owner-liveness metadata to active-folder `list`/`check` output and make `guard-staged` auto-claims either process-bound and auto-reaped when the creating PID is dead, or explicitly short-lived/released after the scoped commit succeeds. Reconcile active-folder release with workboard claim release for pre-commit staged auto-claims, and add focused tests for dead PID pruning, empty-session auto-claims, workboard-release divergence, and process-target coordination messages.
- **Risk:** Medium. The fix affects coordination and commit guard behavior, so it must distinguish stale automated guard leases from real live human/agent folder claims.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with tests first and cleanup behavior gated behind dry-run/reporting before deleting live-looking claims.

### agent-preflight-dirty-worktree-message-points-to-deleted-rules-file

- **Paths:** `scripts/crew/brief/preflight.py`; `tests/test_agent_preflight.py`; `tests/test_new_safety_gates.py`; `AGENTS.md`; `docs/THOMAS_BIBLE.md`; `WORKTREE_RULES.md`
- **Evidence:** `WORKTREE_RULES.md` is absent from the live checkout and `git ls-files WORKTREE_RULES.md` returns no tracked path. `scripts/crew/brief/preflight.py:290-295` still documents `_check_worktree_clean` in terms of `WORKTREE_RULES.md Rule 8`, and the dirty-worktree branches at `scripts/crew/brief/preflight.py:400` and `:409` still emit `See WORKTREE_RULES.md Rule 8.` An isolated temp-git probe with one modified file returned `{'id': 'worktree-clean', 'status': 'blocked', 'message': 'Worktree is dirty: 1 modified. See WORKTREE_RULES.md Rule 8.', ...}` while the live repo still had `worktree_rules_exists=False` and no tracked `WORKTREE_RULES.md`. The current dirty Thomas checkout hits the changed-line budget branch first, so the stale message is easy to miss in live runs. `rg -n "Worktree is dirty|WORKTREE_RULES.md Rule 8|dirty but explicit override" tests scripts -g "*.py"` found only the implementation strings, not a focused assertion that the preflight message points to a live policy source.
- **Impact:** Startup/preflight guidance can send agents to a deleted policy file when they hit ordinary dirty-worktree blocking or dirty override states. That is a poor recovery path at exactly the point where agents need clear coordination instructions, and it partially duplicates the stale `WORKTREE_RULES.md` confusion already visible in the worktree-rules gate.
- **Suggested fix shape:** Reword `_check_worktree_clean` docs/messages to cite the live policy source (`AGENTS.md` and/or the current worktree governance doc) instead of deleted `WORKTREE_RULES.md`. Add focused tests for small dirty worktree, dirty override, and changed-line-budget branches so future message drift is caught. If the Bible still needs historical `WORKTREE_RULES.md` references, keep them explicitly marked historical rather than operational.
- **Risk:** Low. This is message/test cleanup, but it should not weaken the dirty-worktree block or override semantics.
- **Estimated effort:** 1 hour.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow preflight-message/test claim.

### workboard-problem-record-allows-out-of-tree-problem-root

- **Paths:** `scripts/crew/workboard/problem_record.py`; `scripts/crew/tasks/plans.py`; `scripts/forge/gates/workboard_task_problems.py`; `tests/test_workboard_problem_record.py`; `plans/thomas/WORKBOARD.md`
- **Evidence:** The in-progress restored `problem_record.py` imports `_default_problem_path` and `_normalize_artifact_path` from `scripts/crew/tasks/plans.py`, where `_artifact_root_path` accepts an absolute `problem_root` and `_repo_relative_or_absolute` returns an absolute path when the target is outside `ROOT`. `_problem_abs` in `problem_record.py` then accepts that absolute path. A temp workboard probe with one valid active task and `problem_root` set to `C:\Users\corbe\AppData\Local\Temp\...\outside-problems` returned `ok: True`, created `outside-problems\models-lane\PROBLEM.md`, and wrote a `Task Problems` mapping with an absolute `problem=...outside-problems...\PROBLEM.md`. Only a subsequent `workboard_task_problems.evaluate()` call rejected it with `problem path must stay under plans/thomas/problems/`. Current `tests/test_workboard_problem_record.py` covers the normal relative `plans/thomas/problems` root but does not cover absolute or escaping `--problem-root` input.
- **Impact:** The problem-record CLI can claim success while creating problem artifacts outside the canonical repo-local problem tree and leaving `WORKBOARD.md` in a state that the task-problems gate rejects. That makes failure recording unreliable when a caller passes a bad root, and it can dirty or create files outside the expected workboard-owned directory before validation catches the bad mapping.
- **Suggested fix shape:** Reject absolute or escaping `--problem-root` values before writing, or normalize them only if they remain under `plans/thomas/problems/`. Validate the candidate `problem_path` against the same prefix/end-marker rules as `workboard_task_problems` before `_ensure_problem_file` writes anything. Add focused tests for absolute outside roots, `..` roots, normal relative roots, and existing valid mappings.
- **Risk:** Low-medium. The fix is localized to problem-record/workboard artifact validation, but it should preserve any intentional advanced caller that passes an absolute path inside the repo.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow validation/test claim and temp-directory probes only.

### tool-risk-secret-precedence-drops-shell-path-sandbox-control

- **Paths:** `thomas/agent/tool_risk.py`; `tests/test_agent_tool_risk.py`; `plans/thomas/WORKBOARD.md`
- **Evidence:** The in-progress untracked tool-risk classifier has `_RULES` ordered with `secret_access` before `shell_execution`, and `classify_tool_action` chooses `max(candidates, key=lambda item: _RISK_ORDER[item.risk_level])` before applying the shell destructive-command escalation. Because `secret_access` starts as `CRITICAL` and `shell_execution` starts as `HIGH`, a destructive shell command that also mentions a secret-like path selects `secret_access` and never reaches the shell escalation block. A direct probe returned `shell.run {'command': 'Remove-Item -Recurse .env'} => category=secret risk=critical rule=secret_access controls=['deny_by_default', 'manual_approval', 'secret_isolation', 'redact_output', 'audit_log'] has_path_sandbox=False`, while `powershell {'command': 'Remove-Item -Recurse runtime/cache'}` correctly returned `rule=shell_execution` with `path_sandbox=True`. `tests/test_agent_tool_risk.py` covers destructive shell without secret markers and secret file reads, but it does not cover overlapping secret+destructive shell inputs. `plans/thomas/WORKBOARD.md:356-357` shows the tool-risk files are an active dirty coordination lane, so this is an issue candidate rather than a direct fix in this heartbeat.
- **Impact:** A governance caller could receive critical secret controls for an action that is also a destructive shell operation, but miss the path sandbox control that normal destructive shell classification recommends. That weakens policy translation for exactly the mixed high-risk case of deleting, moving, or rewriting secret-bearing paths via shell.
- **Suggested fix shape:** Make classification merge controls across all matched high-risk rules, or add an explicit precedence/tie-breaker for destructive shell/file mutation markers so shell path-sandbox controls are retained when secret markers also match. Add focused tests for `Remove-Item .env`, `rm -rf .env`, non-destructive secret reads, and destructive non-secret shell commands to lock the intended combined-control behavior.
- **Risk:** Medium. The fix is in a governance classifier under active coordination; changing precedence can affect downstream policy expectations, so controls should be additive rather than downgrading secret isolation.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to the tool-risk lane with coordination acknowledged and tests added before policy wiring.

### server-routes-readme-misstates-mission-api-prefixes

- **Paths:** `thomas/server/routes/README.md`; `thomas/server/routes/mission.py`; `thomas/server/routes/mission_benchmark_routes.py`; `tests/test_server_mission_control.py`; `tests/test_operator_mission_smoke.py`
- **Evidence:** `thomas/server/routes/README.md:35-41` lists mission route modules with endpoint prefixes `/mission/*`, `/support/*`, `/tasks/*`, `/workflows/*`, `/benchmark/*`, and `/content/*`. Live route registration disagrees: `thomas/server/routes/mission.py:121-144` registers `/api/mission/control`, `/api/mission/content-hub`, `/api/mission/stream`, `/api/mission/jobs`, `/api/mission/autopilot/...`, `/api/mission/approvals/...`, and `/api/mission/alerts/notify`; `thomas/server/routes/mission_benchmark_routes.py:349-357` registers `/api/mission/benchmarks/...`. Focused tests also assert the `/api/mission/...` contract: `tests/test_server_mission_control.py` calls `/api/mission/jobs`, `/api/mission/benchmarks/packs`, `/api/mission/benchmarks/runs`, and related auth cases, while `tests/test_operator_mission_smoke.py:52-58` includes `/mission`, `/api/mission/control`, `/api/mission/jobs`, and `/api/mission/benchmarks/run`. The queue already has stale chat/loop docs for `thomas/server/routes/README.md`, but that item targets deleted `chat_aiohttp_part*.py` paths, not the mission API prefix table.
- **Impact:** Agents using the routes README as a navigation map can search for or test nonexistent mission endpoints such as `/tasks/*`, `/workflows/*`, `/benchmark/*`, and `/content/*`, missing the real `/api/mission/...` surface and wasting time debugging false route failures.
- **Suggested fix shape:** Update the route README mission rows to reflect the current public prefixes: `/mission` for the page, `/api/mission/control`, `/api/mission/jobs`, `/api/mission/autopilot/*`, `/api/mission/approvals/*`, `/api/mission/content-hub`, `/api/mission/alerts/*`, and `/api/mission/benchmarks/*`. Add a small docs consistency test that extracts route literals from the mission route modules or checks the README for the canonical `/api/mission` prefixes.
- **Risk:** Low. Documentation/test-only cleanup; avoid changing mission route behavior.
- **Estimated effort:** 1 hour.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a docs-only claim plus a lightweight consistency test.

### chat-quick-reply-handler-shadowed-by-noop-streaming-copy

- **Paths:** `thomas/server/routes/chat_aiohttp_streaming.py`; `thomas/server/routes/chat_modes.py`; `docs/CHAT_EXECUTION_MODEL.md`; `thomas/server/routes/README.md`; `thomas/chat/README.md`; `tests/test_server_session_locking.py`; `tests/test_chat_conversation_fixes.py`
- **Evidence:** The live chat route in `thomas/server/routes/chat_aiohttp_streaming.py:345-357` calls a local `maybe_handle_quick_casual_reply(...)` before dispatch routing, but the local function at `thomas/server/routes/chat_aiohttp_streaming.py:838-849` is still a placeholder docstring and always returns `None`. A second implementation with the same name exists in `thomas/server/routes/chat_modes.py:40-72`; that module documents the fast casual handler but also returns `None` intentionally, and `rg -n "chat_modes|maybe_handle_quick_casual_reply|from \\.chat_modes"` found no import from `chat_modes.py` into the active streaming route. Docs still advertise the helper as a fast-reply surface: `docs/CHAT_EXECUTION_MODEL.md:14-20` says casual messages get a direct fast response with no tools, `docs/CHAT_EXECUTION_MODEL.md:39` lists `chat_modes.py` as the fast-reply handler, `thomas/server/routes/README.md:24` says `chat_modes.py` owns "Fast reply vs normal mode logic", and `thomas/chat/README.md:211-218` says CASUAL quick replies happen immediately with no LLM call. The focused tests only assert the string exists in `chat_modes.py` (`tests/test_server_session_locking.py:65`) and `tests/test_chat_conversation_fixes.py:10` notes quick reply tests needing aiohttp/server imports are skipped when unavailable; no test asserts that the active `/api/chat` route uses the documented fast-reply helper or that casual inputs avoid the full dispatch/agent path.
- **Impact:** Thomas has a documented quick casual reply path that is either dead or shadowed by a no-op duplicate in the active route. Operators and agents can believe simple greetings/thanks are served by a fast path while the live code always falls through to heavier dispatch/agent handling, making chat latency, tool policy, and docs harder to reason about.
- **Suggested fix shape:** Decide whether instant no-LLM casual replies are still part of the product contract. If yes, import/reuse one canonical helper from `chat_modes.py` or move the active implementation there, then add an aiohttp-level test showing a greeting/thanks returns the fast casual response without entering dispatch/workboard flow. If no, remove the placeholder hook and update `docs/CHAT_EXECUTION_MODEL.md`, `thomas/server/routes/README.md`, and `thomas/chat/README.md` so they describe the current dispatch-first casual path accurately. In either case, replace the source-string assertion in `tests/test_server_session_locking.py` with behavior or import wiring coverage.
- **Risk:** Medium. Implementing the fast path can change chat behavior and personality/tone, while removing it requires careful docs and tests so agents stop expecting no-LLM replies.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, if scoped to choosing and testing one canonical quick-reply contract before changing route behavior.

### soak-runner-ignores-failed-failure-injection-command

- **Paths:** `thomas/system/soak_runner.py`; `scripts/soak_runner.py`; `docs/ops/FOCUS_PROGRAM_OPERATING_MODEL.md`; `docs/THOMAS_BIBLE.md`; `tests/test_system_soak_runner.py`; `tests/test_system_health.py`
- **Evidence:** `docs/ops/FOCUS_PROGRAM_OPERATING_MODEL.md:43` presents the Wednesday reliability command as `python scripts/soak_runner.py --command "python -m thomas.cli.main chat \"health\"" --iterations 500 --inject-failure-every 25 --failure-command "python scripts/repair.cmd" --json`, but `scripts/repair.cmd` is absent and untracked (`Test-Path scripts/repair.cmd` false, `git ls-files -- scripts/repair.cmd` empty). The Bible still says `repair.cmd` is clean and wraps a same-named PowerShell script at `docs/THOMAS_BIBLE.md:529` and `:753`. More importantly, the soak runner does not make failed injection commands affect the result: `thomas/system/soak_runner.py:99-117` records `inject_result.returncode` only in JSONL, while `thomas/system/soak_runner.py:213-224` reports only primary-command failures and an `injected_failures` count. A focused probe with a temp log, `python scripts/soak_runner.py --command 'python -c "print(123)"' --iterations 1 --inject-failure-every 1 --failure-command 'python scripts/repair.cmd' --log-file <temp> --json`, exited 0 and printed `"ok": true`, `"success_count": 1`, `"failure_count": 0`, and `"injected_failures": 1`; the JSONL side log recorded the failure-injection row with `"command": "python scripts/repair.cmd"` and `"exit_code": 2`. `tests/test_system_soak_runner.py:11-39` only asserts the injected count and primary failure count, and `tests/test_system_health.py` covers probe failures with `failure_command=""`, so no focused test requires failed injection commands to fail or warn in the aggregate report.
- **Impact:** The documented reliability proof can report green even when the fault-injection/repair command is missing or broken. That weakens soak evidence exactly for the recovery path the focus-program operating model is trying to validate, and it lets a missing Windows repair wrapper hide behind a successful primary health command.
- **Suggested fix shape:** Track injection success/failure separately in `run_soak` summary, include the last injection exit code/error in JSON output, and make the CLI fail by default when any injection command exits nonzero unless an explicit `--allow-injection-failures` option is set. Add tests for missing/nonzero failure commands, successful injection commands, default CLI exit code, and any opt-out behavior. Update the focus-program command and Bible wrapper inventory so they point at a live repair entrypoint or mark the wrapper as absent.
- **Risk:** Medium. Tightening failure-injection semantics may break existing soak jobs that intentionally inject failures via nonzero commands, so the remediation should distinguish "fault injected successfully" from "injection command failed to run" and provide a deliberate compatibility escape hatch.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with focused soak-runner tests and docs cleanup; avoid running the 500-iteration production command during remediation.

### bible-installer-wrapper-inventory-names-missing-support-assets

- **Paths:** `docs/THOMAS_BIBLE.md`; `docs/WINDOWS_INSTALLER_GUIDE.md`; `build-installer.cmd`; `scripts/build_windows_installer.ps1`; `scripts/setup.ps1`; `scripts/repair.ps1`; `scripts/bootdoctor.ps1`; `scripts/run-ui.ps1`; `tests/test_launcher_boot_recovery_contract.py`; `tests/test_setup_wizard.py`
- **Evidence:** The Bible's installer section says there are tests in `tests/test_setup_wizard.py`, `tests/test_windows_installer_assets.py`, and `tests/test_launcher_boot_recovery_contract.py`, and that the "repair, bootdoctor, and support paths all have dedicated tests" at `docs/THOMAS_BIBLE.md:522`. Live path checks show `tests/test_setup_wizard.py` and `tests/test_launcher_boot_recovery_contract.py` exist, but `tests/test_windows_installer_assets.py` is absent and untracked. The same section says `setup.cmd`, `repair.cmd`, `bootdoctor.cmd`, `support.cmd`, `run-ui.cmd`, `run-repl.cmd`, and `build-installer.cmd` all wrap a same-named `.ps1` in `scripts/` at `docs/THOMAS_BIBLE.md:528-529`; live wrapper checks show root `support.cmd` is absent/untracked, `scripts/support.ps1` is absent/untracked, and `scripts/build-installer.ps1` is absent/untracked. The actual tracked build wrapper is `build-installer.cmd`, and it calls `scripts/build_windows_installer.ps1`, not a same-named script. The file table also lists `scripts/support_bundle.ps1` as canonical at `docs/THOMAS_BIBLE.md:564`, but that file is absent and untracked in the live checkout. `docs/WINDOWS_INSTALLER_GUIDE.md:47-48` lists only `run-ui.cmd` and `repair.cmd` as installer contents, so the broader support wrapper claims are concentrated in the Bible rather than covered by current installer docs.
- **Impact:** The source-of-truth Bible overstates Windows installer/support coverage and sends agents toward missing support wrapper assets and a missing installer-assets test. That weakens confidence in the launcher/install audit section and can cause future agents to assume support-bundle and wrapper-pair behavior is protected when the files are no longer present.
- **Suggested fix shape:** Re-audit the Windows installer/support section against the current tracked root `.cmd` and `scripts/*.ps1` inventory. Either restore the intended `support.cmd`/support-bundle path and installer-assets test, or update the Bible to mark those assets retired and describe the real `build-installer.cmd` to `scripts/build_windows_installer.ps1` exception. Add a focused docs/inventory test that checks current Bible wrapper-pair examples refer to tracked files, while allowing explicitly documented exceptions like `build-installer.cmd`.
- **Risk:** Low. This is primarily source-of-truth documentation and coverage inventory cleanup, but restoring a support bundle would touch user-facing Windows support assets.
- **Estimated effort:** 1-2 hours.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a docs/test-only claim unless the chosen fix restores missing support assets.

### scripts-readme-documents-removed-auto-check-flags

- **Paths:** `scripts/README.md`; `scripts/auto_checks.py`; `scripts/forge/gates/monolith_guard.py`; `tests`
- **Evidence:** `scripts/README.md:282-300` tells operators to run `python scripts/forge/gates/monolith_guard.py --fix` from both the command-line and "Fix issues automatically" examples, and `python scripts/auto_checks.py --checks monolith,release` for "Specific checks only". The current CLIs do not accept those flags: `python scripts/forge/gates/monolith_guard.py --help` lists only `--repo-root`, `--baseline`, `--base`, `--head`, `--staged-only`, and `--json`, and running `python scripts/forge/gates/monolith_guard.py --fix` exits 2 with `error: unrecognized arguments: --fix`. `python scripts/auto_checks.py --help` lists `--quick`, clean-dev-artifact options, skip/test/problem-record flags, clean-worktree flags, and `--continue-on-fail`, but no `--checks`; running `python scripts/auto_checks.py --checks monolith,release` exits 2 with `error: unrecognized arguments: --checks monolith,release`. `rg -n "scripts/README|monolith_guard\\.py --fix|auto_checks\\.py --checks|Running Scripts|Specific checks only" tests ...` found only the README examples and no test that command examples in `scripts/README.md` match current parser help.
- **Impact:** Agents and operators following the scripts directory README hit immediate argument-parser failures for basic local-check workflows. The stale `--fix` example is especially misleading because monolith guard is a diagnostic gate, not an auto-fixer, and the stale `--checks` example implies selective auto-check execution that the current runner no longer supports.
- **Suggested fix shape:** Update `scripts/README.md` to remove unsupported `--fix` and `--checks` examples or replace them with current supported equivalents, such as `monolith_guard.py --json`, `--staged-only`, `auto_checks.py --quick`, or `--skip-*` only where breakglass rules allow it. If selective checks are still desired, add a deliberate `--checks` implementation with tests rather than advertising a nonexistent flag. Add a lightweight docs command-example test for `scripts/README.md` snippets or a table of supported script examples so parser drift is caught.
- **Risk:** Low. Documentation/test-only cleanup unless reintroducing selective auto-check execution.
- **Estimated effort:** 1 hour.
- **Date found:** 2026-06-26.
- **Suitable for autonomous worker:** Yes, with a narrow docs/test claim.

### chat-canvas-live-store-never-evicts-finished-html

- **Paths:** `thomas/server/chat_delegation_canvas.py`; `thomas/server/chat_delegation.py`; `thomas/server/chat_delegation_session.py`; `tests/test_chat_delegation_canvas.py`; `tests/test_chat_canvas_live_preview.py`
- **Evidence:** The in-progress canvas worker keeps live rendered HTML in a module-level process-local `_STORE` at `thomas/server/chat_delegation_canvas.py:26`. `canvas_start` inserts a per-execution row at `:35-37`, `canvas_append` keeps concatenating streamed tokens at `:40-43`, `canvas_set_html` replaces the row with final HTML at `:47-50`, and `canvas_finish` only changes the stored `status` at `:54-57`. `canvas_get` at `:61-64` returns a copy of the stored row but there is no `pop`, delete, TTL, size limit, or cleanup API for `_STORE`; the only `pop` in the module is `_LLM_CACHE.pop` at `:179`. The active delegation path calls `canvas_start` before emitting the first event in `thomas/server/chat_delegation.py:382-384`, then `run_canvas_worker` calls `canvas_start` again at `thomas/server/chat_delegation_canvas.py:196`, appends tokens, stores the final HTML, and marks status `"done"` or `"failed"` at `:286-290`. `thomas/server/chat_delegation_session.py:44-63` reads `_STORE` for every normalized delegation and surfaces `canvas_html` when present, including terminal executions. Current focused tests assert that rendered HTML remains in `canvas_get("exec-canvas-stream")` after completion (`tests/test_chat_delegation_canvas.py:41-58`) and that the frontend can render streaming HTML (`tests/test_chat_canvas_live_preview.py:7-12`), but no test covers eviction after completion/failure, maximum retained bytes, or bounded completed-execution retention.
- **Impact:** Every visual/canvas delegation can leave the full generated HTML resident in the web process for the lifetime of the server, even after the task is completed or failed and the deliverable is persisted to disk. Long-running Thomas sessions that generate many visuals can accumulate memory and stale canvas payloads, and the `/delegations` normalization path keeps treating any retained row as live canvas metadata.
- **Suggested fix shape:** Add an explicit retention policy for canvas rows: keep streaming rows until completion, retain completed/failed HTML only for a short TTL or a bounded LRU count, and provide a cleanup path after the frontend has a persisted deliverable URL. Store metadata needed to mark a terminal execution as canvas separately from the full HTML, or trim `canvas_html` once `artifact_url` is available. Add tests for completion cleanup, failure cleanup, bounded retention, and continued live streaming before completion.
- **Risk:** Medium. Evicting too aggressively can break the live preview experience if the frontend polls late, so the fix should preserve streaming behavior and make terminal retention deliberate.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-27.
- **Suitable for autonomous worker:** Yes, if coordinated with the active chat canvas lane and tested with focused server/unit coverage.

### unclaimed-dirty-slice-owner-attribution-gap

- **Paths:** `plans/thomas/WORKBOARD.md`; `scripts/crew/workboard/claim.py`; `scripts/active_folders.py`; `thomas/forge/anvil/native_orchestration.py`; `tests/test_native_orchestration.py`; `thomas/core/autonomy.py`; `thomas/core/send_task_tool.py`; `thomas/marketplace/orchestrator/brain.py`; `thomas/marketplace/specialists/reasoning.py`; `thomas/server/chat_delegation.py`; `thomas/server/chat_delegation_emitter.py`; `thomas/server/chat_delegation_session.py`; `thomas/server/routes/chat_v2.py`; `thomas/server/routes/core_aiohttp.py`; `thomas/server/web/index.html`; `thomas/server/web/chat.html`; `thomas/server/web/thomas_chat.html`; `thomas/server/web/css/thomas_world.css`; `thomas/server/web/js/thomas_world.js`
- **Evidence:** On 2026-06-27, repeated workboard messages reported a large dirty chat/canvas/autonomy slice with no visible owner. `plans/thomas/WORKBOARD.md:385` records a stale merge-coordinator blocker where live checks had `active workboard claims=0`, `active_folders leases=0`, no new commits since `3857df63`, and dirty/untracked files across `thomas/core/autonomy.py`, `thomas/core/send_task_tool.py`, `thomas/server/app_lifecycle.py`, `thomas/server/chat_delegation*.py`, `thomas/server/routes/chat_v2.py`, and route/web assets. `plans/thomas/WORKBOARD.md:386` asks native orchestration to detect hidden-owner dirty slices and force owner assignment, quarantine, or claim creation before unrelated commits proceed. `plans/thomas/WORKBOARD.md:387` repeats the same owner-attribution gap from the merge coordinator. A fresh 2026-06-27T16:19Z check still showed `python scripts/crew/workboard/claim.py --list --json` with `active_claim_count: 0`, while `git status --short --branch` still listed the dirty chat/canvas/autonomy files and untracked canvas tests/assets. The only active-folder lease at that check was unrelated (`codex-agentic-genai-otel-schema` on `tests/test_agent_telemetry_schema.py` and `thomas/agent/telemetry_schema.py`) and its PID was already dead, so it did not explain ownership of the chat/canvas slice.
- **Additional evidence (2026-06-27T21:53Z):** Pipeline Observer reported the hidden-owner blocker still repeating while `run_thomas_main_8906.py` stayed alive under PIDs 52812 and 53280. A live check confirmed both PIDs were running `python`, `git status --short` still showed dirty/untracked chat/canvas/web files including `thomas/server/chat_delegation.py`, `thomas/server/routes/chat_v2.py`, `thomas/server/routes/core_aiohttp.py`, `thomas/server/web/index.html`, `thomas/server/chat_delegation_canvas.py`, `thomas/server/web/chat.html`, `thomas/server/web/css/thomas_world.css`, `thomas/server/web/js/thomas_world.js`, and `thomas/server/web/thomas_chat.html`, and `_real_server_8906.log` still recorded permanent delegation event drops after closed-transport write failures. `python scripts/crew/workboard/claim.py --list --json` and `python scripts/active_folders.py list --json` both reported no active ownership during the same heartbeat.
- **Additional evidence (2026-06-28T01:48Z):** Pipeline Observer reported a second unowned product-capability slice: tracked dirty edits added first-class inline `remember`/`recall` memory tooling across `thomas/core/send_task_tool.py`, `thomas/marketplace/orchestrator/brain.py`, and `thomas/marketplace/specialists/reasoning.py` while `python scripts/crew/workboard/claim.py --list --json` and `python scripts/active_folders.py list --json` both reported no active ownership. A live check confirmed those three files were dirty and contained the new memory-tool prompts/callback wiring, so the owner-attribution dashboard needs to cover non-server orchestration capability edits too, not just running server processes.
- **Additional evidence (2026-06-28T02:27Z):** Pipeline Observer reported an unowned generated-artifact/probe class: root `_codex_rundown.md` and `_readiness_battery.py` existed without a visible workboard claim, and a dead-PID `codex-parallel-code-worker-ui` active-folder lease had to be cleaned after its doc slice was committed. A later live check found those root artifacts already gone and `python scripts/active_folders.py list --json` clear, but the native guardrail still needs to classify transient root reports/probes and post-commit dead leases so they are either claimed, quarantined, attached to evidence logs, or cleaned deliberately.
- **Additional evidence (2026-06-28T03:07Z):** Pipeline Observer reported the shared checkout itself had been switched from `dev` to `claude/chat-sweep-2026-06-27` with no workboard claims or active-folder leases. A live check confirmed the branch mismatch while `dev`, `HEAD`, and `claude/chat-sweep-2026-06-27` pointed at the same commit, plus staged evolve_auto/nightshift renames and new autonomous-runner files (`plans/thomas/evolve_auto/NIGHTSHIFT_DESIGN.md`, `SEED_BACKLOG.md`, `loop.js`, `loop_round.js`) and unstaged chat/server edits (`thomas/marketplace/specialists/reasoning.py`, `thomas/server/app_middleware_handlers.py`, `thomas/server/web/chat.html`). Native owner-attribution needs to detect branch takeover plus staged/unstaged slice ownership, not just dirty paths on the expected branch.
- **Impact:** Thomas can accumulate a large dirty product slice that is neither claimed on the visible workboard nor represented by active-folder ownership. Merge coordination then cannot tell whether the files are abandoned, owned by a hidden thread, ready for takeover, or unsafe to commit. Native orchestration dashboards would show clean claims/leases while the worktree contains high-impact unowned changes, which invites unrelated commits to trample or strand the slice.
- **Suggested fix shape:** Add native owner-attribution checks that compare dirty/untracked/staged files, branch identity, workboard claims, active-folder leases, recent commit trailers, and open workboard messages. Surface an "unclaimed dirty slice" dashboard/card with affected paths, age, suspected owner evidence, branch status, and actions: require a claim, return to the expected branch, quarantine from unrelated scoped commits, message the merge coordinator, or create a takeover task. Include generated root reports/probes and post-commit dead leases in the same lifecycle view, with explicit keep/attach/delete recommendations instead of silent drift. Extend `native_orchestration.py` with dirty-file ownership summaries and recommendations, then add tests for unclaimed dirty files, dirty files covered by a workboard claim, dirty files covered by active-folder leases, generated root artifacts, branch mismatch, and stale/dead-owner exceptions.
- **Risk:** Medium. Ownership inference can create false positives if a human is intentionally editing without a claim, so the first implementation should be advisory/reporting and should not block commits until the policy is explicit.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-27.
- **Suitable for autonomous worker:** Yes, if scoped to reporting and tests first; enforcement should wait for a clear product-owner policy on quarantine/takeover behavior.

### chat-delegation-events-drop-after-stream-eof

- **Paths:** `thomas/chat/event_stream.py`; `thomas/server/routes/chat_v2.py`; `thomas/server/chat_delegation.py`; `thomas/server/chat_delegation_emitter.py`; `thomas/server/chat_delegation_session.py`; `tests/test_chat_delegation.py`; `tests/test_realtime_ws.py`; `plans/thomas/chat_stress_2026-06-17/FIXES.md`
- **Evidence:** A 2026-06-27T17:48Z pipeline-observer heartbeat inspected the active `run_thomas_main_8906.py` server output in `_real_server_8906.log` and found repeated lines: `Event permanently dropped after retry: delegation_progress (event type=Cannot call write() after write_eof(), error=Cannot call write() after write_eof())`, followed by a dropped `delegation_completed` event with the same `write_eof` error. The running process set was still the chat/canvas server lane (`python run_thomas_main_8906.py` under PIDs 33516 and 41664, plus a bash relaunch wrapper at PID 50324), while `python scripts/crew/workboard/claim.py --list --json` and `python scripts/active_folders.py list --json` both reported no active visible ownership. This is not a one-off unknown: `plans/thomas/chat_stress_2026-06-17/FIXES.md` already documents an open stress finding that `delegation_completed` can be emitted onto a chat-turn stream after `write_eof()`, with page reloads failing to reattach artifact chips to historical completed tasks. `rg` found no matching entry in `THOMAS_CODEBASE_ISSUE_QUEUE.md` or `THOMAS_CODEBASE_ISSUE_RANKINGS.md` before this item was added.
- **Impact:** Live delegation progress/completion can be lost exactly when the stream has already closed. The poll path may reconcile some live strips, but dropped terminal events leave users and orchestration dashboards vulnerable to stale in-progress state, missing artifact chips, and worker completions that only become visible through indirect recovery paths. Because the failure appears in the active server log during ongoing canvas/chat work, it is a current coordination-pipeline reliability issue, not just an archived stress note.
- **Suggested fix shape:** Make the delegation event sink aware of closed streams and route post-EOF worker events to the durable delegation ledger/session state before attempting live stream writes. Treat live stream delivery as best-effort fanout, not the source of truth for terminal delegation state. Add focused tests that close a chat response before `delegation_progress` and `delegation_completed`, assert no permanent-drop log for expected post-EOF delivery, and verify `/delegations` or conversation reload reconstructs terminal status and artifact chips from durable state. Keep any stream-write retry bounded and quiet for known closed-stream cases so real event transport failures still surface.
- **Risk:** Medium. Changing stream/ledger ordering can affect live chat behavior and artifact visibility, so start with post-EOF tests and a durable-state replay fix before changing broader delegation dispatch.
- **Estimated effort:** 2-4 hours.
- **Date found:** 2026-06-27.
- **Suitable for autonomous worker:** Yes, with a focused chat delegation reliability claim and tests around closed streams, terminal status replay, and artifact-chip reconstruction.
