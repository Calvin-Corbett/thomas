import { RouteLoadingShell } from "@/components/system-route-state";

export default function JourneyLoading() {
  return (
    <RouteLoadingShell
      eyebrow="Journey"
      title="Architecture and production maturity"
      intro="Loading the timeline and scope notes."
      versionLabel="Loading"
    />
  );
}
