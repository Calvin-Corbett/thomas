import { RouteLoadingShell } from "@/components/system-route-state";

export default function SupportLoading() {
  return (
    <RouteLoadingShell
      eyebrow="Support"
      title="Issue reporting and troubleshooting"
      intro="Preparing support guidance and intake instructions."
      versionLabel="Loading"
    />
  );
}
