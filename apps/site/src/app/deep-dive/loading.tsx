import { RouteLoadingShell } from "@/components/system-route-state";

export default function DeepDiveLoading() {
  return (
    <RouteLoadingShell
      eyebrow="Engineering Deep Dive"
      title="Execution architecture and command contracts"
      intro="Loading deep-dive sections and command-matrix data."
      versionLabel="Loading"
    />
  );
}
