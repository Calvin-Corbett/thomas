# Prompt pack tests

Per-feature tests named `test_pNNN_<slug>.py`. Each one covers the success
path, an invalid-input path, and the stable output contract for its feature
(JSON shapes in particular).
