diff --git a/thomas/server/app.py b/thomas/server/app.py
index 0000000..1111111 100644
--- a/thomas/server/app.py
+++ b/thomas/server/app.py
@@
+# Time-Travel Debugger wiring (Runs store + /api/chat recorder)
+# NOTE: This diff is a guide. Apply manually if your app.py differs.
+
+import json
+import time
+import contextlib
+from thomas.observability import run_store
+from thomas.server.routes.runs import register_runs_routes
@@
 def create_app(...):
     app = web.Application(...)
     config = load_config(...)
+    app["config"] = config
+    register_runs_routes(app, config)
@@
 async def api_chat(request):
     # Existing /api/chat handler streaming NDJSON
-    ...
+    body = await request.json()
+    session_id = body.get("session_id") or body.get("sessionId")
+    profile = body.get("profile")
+    model_id = body.get("model_id") or body.get("modelId")
+    mode = body.get("mode")
+
+    run_id = run_store.create_run({
+        "session_id": session_id,
+        "profile": profile,
+        "model_id": model_id,
+        "mode": mode,
+        "thomas_version": getattr(config, "version", None) if not isinstance(config, dict) else config.get("version"),
+    })
+    writer = run_store.ThreadedRunWriter(run_id)
+
+    run_start = {
+        "type":"run_start",
+        "run_id": run_id,
+        "session_id": session_id,
+        "profile": profile,
+        "model_id": model_id,
+        "mode": mode,
+        "ts": time.time(),
+    }
+    writer.record(run_start)
+    await resp.write((json.dumps(run_start) + "\n").encode("utf-8"))
+
+    ok = True
+    error = None
+    iterations = None
+    tool_calls = None
+    usage = None
+    try:
+        async for evt in stream_events_from_llm(...):
+            if isinstance(evt, dict) and "run_id" not in evt:
+                evt["run_id"] = run_id
+            writer.record(evt)
+            await resp.write((json.dumps(evt) + "\n").encode("utf-8"))
+            if isinstance(evt, dict) and evt.get("type") == "done":
+                iterations = evt.get("iterations", iterations)
+                tool_calls = evt.get("tool_calls", tool_calls)
+                usage = evt.get("usage", usage)
+    except Exception as e:
+        ok = False
+        error = str(e)
+        err_evt = {"type":"error","run_id":run_id,"message":error}
+        writer.record(err_evt)
+        await resp.write((json.dumps(err_evt) + "\n").encode("utf-8"))
+        raise
+    finally:
+        with contextlib.suppress(Exception):
+            writer.close()
+        run_store.finalize_run(run_id, ok=ok, error=error, iterations=iterations, tool_calls=tool_calls, usage=usage)
