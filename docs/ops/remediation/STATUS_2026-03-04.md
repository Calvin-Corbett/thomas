# Remediation Status (2026-03-04)

This document captures **post-remediation** validation status after the local baseline capture.

## Collection Gate Status

Command:
- `pytest --collect-only -q`

Result:
- PASS
- `10329 tests collected in 7.51s`
- Collection/import-time bootstrap failures are resolved.

## Required Repo Gates

Command:
- `python scripts/check_monolith_guard.py`

Result:
- PASS
- `Monolith guard OK. Scanned 4022 files under .`

Command:
- retired in current runtime policy

Result:
- PASS
- Thomas now boots directly from `thomas/server/web/js/app_runtime_primary.mjs`; the joined-runtime build path was removed.

Command:
- `python scripts/check_feature_registry.py`

Result:
- PASS
- `Feature registry check: PASS (14 domain path entries)`

Command:
- `python scripts/check_deletions.py --staged-only`

Result:
- PASS
- `Deletion guard: PASS` (no protected-path deletions detected)

Command:
- `python scripts/check_release_hygiene.py`

Result:
- PASS
- Current version header present in `CHANGELOG.md`.
- Non-fatal warnings remain for low telemetry sample and one security-audit warning.

## Targeted Regression Tests (fixed in this pass)

Command:
- `pytest -q tests/test_agent_loop_library.py`
- `pytest -q tests/test_agent_loop_memory_and_tokens.py::TestAgentLoopMemoryAndTokens::test_route_applies_thread_memory_policy_for_casual_chat`
- `pytest -q tests/test_agent_loop_tool_policy.py`
- `pytest -q tests/test_agent_skills_runtime.py::test_agent_loop_injects_runtime_skill_context_into_system_prompt`
- `pytest -q tests/test_architecture.py::test_file_sizes`

Result:
- PASS

## Full `pytest -q` Run Note

A full-suite run was executed with output redirected to `.tmp_pytest_q_full_continue.out`.

Observed behavior:
- The run progressed to ~79% with many assertion failures (beyond collection scope).
- It also spawned long-running workboard monitor subprocesses and entered slow/hanging tails.
- The run was manually terminated after capturing enough evidence that:
  - collection is stable,
  - execution proceeds deep into the suite,
  - remaining issues are behavioral test failures and long-running/hanging test paths (not collection/bootstrap errors).

## Remaining Known Risk Areas

- Large backlog of behavioral failures in full-suite execution.
- Architecture policy suite currently surfaces broad debt/registry mismatches not addressed in this remediation pass.
- Some test paths spawn monitor subprocesses that can extend or stall run completion.
