/*
 * composer_redesign.js — RETIRED by the ground-up composer rebuild.
 *
 * The composer markup now ships its final icons natively and the look is owned
 * entirely by css/composer.css; the controls chrome lives in js/composer_controls.js.
 * This file's old body (actions-icon MutationObserver + send-hint injector) is
 * obsolete. Its <script> tag was removed from index.html. Kept as a no-op so any
 * stale cached reference resolves harmlessly.
 */
(function () {
  'use strict';
})();
