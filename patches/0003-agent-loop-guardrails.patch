diff --git a/thomas/agent/loop.py b/thomas/agent/loop.py
index 0000000..0000000 100644
--- a/thomas/agent/loop.py
+++ b/thomas/agent/loop.py
@@
+# --- GUARDRAILS BEGIN ---
+from thomas.policy.config import load_policy_config
+from thomas.policy.policy import PolicyEngine
+from thomas.policy.redact import Redactor
+from thomas.agent.approval import ApprovalBroker
+from thomas.agent.guarded_tools import GuardedToolRunner
+# --- GUARDRAILS END ---
@@
+# --- GUARDRAILS BEGIN ---
+# In AgentLoop __init__ accept optional:
+#   guardrails_cfg=None, guardrails_approvals=None, guardrails_audit=None, guardrails_redactor=None
+# and build:
+#   self._guardrails_enabled = bool(cfg.guardrails.enabled)
+#   self._guarded_tools = GuardedToolRunner(policy=PolicyEngine.from_config(cfg), approvals=broker, redactor=redactor, audit=audit, approval_timeout_s=cfg.guardrails.approval_timeout_s)
+# --- GUARDRAILS END ---
@@
+# --- GUARDRAILS BEGIN ---
+# Around the tool execution site, replace:
+#   result = await self.tools.execute(tool_call)
+# with:
+#   if self._guardrails_enabled:
+#       async def _exec(call):
+#           return await self.tools.execute(call)
+#       async def _emit(t, data):
+#           await self.emit_event(t, data)   # adapt to your event emitter
+#       result = await self._guarded_tools.run(
+#           executor=_exec,
+#           tool_call=tool_call,
+#           run_id=run_id,
+#           session_id=session_id,
+#           iteration=iteration,
+#           cwd=cwd,
+#           sandbox_root=sandbox_root,
+#           runtime_root=runtime_root,
+#           conversation_summary=conversation_summary,
+#           emit_event=_emit,
+#       )
+#   else:
+#       result = await self.tools.execute(tool_call)
+# --- GUARDRAILS END ---
