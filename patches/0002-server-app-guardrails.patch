diff --git a/thomas/server/app.py b/thomas/server/app.py
index 0000000..0000000 100644
--- a/thomas/server/app.py
+++ b/thomas/server/app.py
@@
+# --- GUARDRAILS BEGIN ---
+from pathlib import Path
+from thomas.policy.config import load_policy_config
+from thomas.policy.policy import PolicyEngine
+from thomas.policy.redact import Redactor
+from thomas.agent.approval import ApprovalBroker
+from thomas.server.audit_log import AuditLog
+from thomas.server.guardrails_api import install_guardrails_routes
+# --- GUARDRAILS END ---
@@
+# --- GUARDRAILS BEGIN ---
+# During app startup, after runtime_root is known:
+#   cfg = load_policy_config(runtime_root)
+#   if cfg.guardrails.enabled:
+#       redactor = Redactor(cfg.redact_additional_patterns)
+#       approvals = ApprovalBroker()
+#       audit = AuditLog(Path(runtime_root) / ".thomas" / "audit.sqlite3", redactor)
+#       install_guardrails_routes(app, approvals)
+#       app["guardrails"] = {"cfg": cfg, "redactor": redactor, "approvals": approvals, "audit": audit}
+# --- GUARDRAILS END ---
