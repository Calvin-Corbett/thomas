*** Begin Patch
*** Update File: thomas/agent/loop.py
@@
-            if not best_practice_hint:
-                best_practice_hint = best_practice_gate_hint(prompt_text)
-            library_text = self._retrieve_library(prompt_text, route)
+            if not best_practice_hint:
+                best_practice_hint = best_practice_gate_hint(prompt_text)
+            skip_library_for_coding = str(job_type or ).strip().lower() ==  coding
+ if skip_library_for_coding:
+ library_text = 
+            else:
+                library_text = self._retrieve_library(prompt_text, route)
*** End Patch

